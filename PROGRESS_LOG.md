# سجل تقدّم المشروع (Handoff Log)

> هذا الملف هو نقطة التنسيق بين أي جلسة تعمل على هذا المشروع (سحابية أو من
> جهاز محلي). القاعدة: **اسحب (`git pull`) واقرأ هذا الملف أول شي قبل أي
> عمل، وحدّثه ثم ادفع (`git push`) آخر شي بعد أي دفعة عمل.** التفاصيل في
> `CLAUDE.md`.

## الحالة الحالية (آخر تحديث: 2026-07-23)

- **Backend**: مكتمل 100%. FastAPI + PostgreSQL، RBAC، دورة دفع بتحقق
  يدوي، محفظة وكلاء B2B، تخزين آمن، 19 اختبار pytest ناجح.
- **Frontend**: مكتمل الآن للشاشات الأربع الأساسية — تسجيل الدخول، لوحة
  التحكم، **الطلبات**، **الوكلاء (B2B)**، **العملات وسعر الصرف**. كلها
  مبنية ومُختبرة يدوياً في المتصفح عبر بيانات حقيقية من الـ API. شاشة
  "حسابي" (`/profile`) لا تزال placeholder ("قيد الإنشاء").
- **الفرع الحالي**: `claude/wakalat-bradis-project-7iwyup`.

## ملاحظات إعداد بيئة محلية (مهمة لأي جهاز جديد)

- استخدم **Python 3.13** لبيئة الـ backend الافتراضية (venv)، **ليس**
  Python 3.14 — بعض المكتبات المثبَّتة (`pydantic-core`, `psycopg2-binary`)
  ما عندها wheels جاهزة لـ 3.14 بعد، وبيحاول pip يبنيها من المصدر ويفشل
  بدون Visual Studio Build Tools.
- `psycopg2-binary` في `requirements.txt` مثبَّت على `2.9.10` (بدل
  `2.9.9` الأصلي) لأن هذا الإصدار فيه wheel جاهز لـ Windows/cp313.
- إذا كان عندك أكثر من نسخة PostgreSQL على الجهاز، تأكد من الـ port
  الصحيح في `postgresql.conf` قبل ضبط `DATABASE_URL` في `.env`.
- سكربتات Python اللي فيها `print()` بالعربي (زي
  `scripts/seed_initial_data.py`) تحتاج `PYTHONIOENCODING=utf-8` قبل
  التشغيل على ويندوز، وإلا بترمي `UnicodeEncodeError` بسبب ترميز cp1252
  الافتراضي.
- **تحذير مهم لأي جهاز ويندوز**: نظام الملفات على ويندوز لا يفرّق بين
  الأحرف الكبيرة والصغيرة. تجنّب إنشاء ملفين بنفس الاسم يختلفان بحرف
  واحد فقط (Case) في نفس المجلد — هذا سبب باگ حقيقي كسر التطبيق بالكامل
  (`AuthContext.tsx` تعارض مع `authContext.ts`، تم حله بإعادة التسمية
  إلى `authContextValue.ts`).

## الخطوة القادمة المقترحة

بناء شاشة "حسابي" (`/profile`)، أو مراجعة/تحسين الشاشات الثلاث المبنية
حديثاً (الطلبات، الوكلاء، العملات) حسب ملاحظات فعلية من الاستخدام.

---

## سجل الدفعات (الأحدث فوق)

### 2026-07-23 — إعداد بيئة محلية + بناء شاشات الطلبات/الوكلاء/العملات

- من: جلسة سحابية (Claude on claude.ai)
- تم: تثبيت Python/PostgreSQL محلياً، إعداد venv وقاعدة بيانات وملف
  `.env`، تشغيل الـ backend والـ frontend محلياً، بناء الشاشات
  التفصيلية الثلاث (طلبات، وكلاء B2B، عملات) بالكامل مع اختبار يدوي
  حقيقي في المتصفح، إصلاح باگين (تعارض أسماء ملفات على ويندوز + رصيد
  محفظة لا يتحدّث فوراً بعد الإيداع).
- التالي: دفع هذا العمل لـ GitHub، ثم — حسب توجيه المستخدم.
