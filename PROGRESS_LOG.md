# سجل التقدّم المشترك — وكالة براديس

> هذا الملف هو نقطة التنسيق بين أي جلسة Claude Code تعمل على هذا
> المشروع (سواء من السحابة أو من جهاز محلي). **كل جلسة يجب أن:**
> 1. تعمل `git pull` وتقرأ هذا الملف **قبل** بدء أي عمل جديد.
> 2. تحدّث القسم أدناه بعد **كل** دفعة عمل منجزة (commit/push)، وتضيف
>    سطراً جديداً في "السجل الزمني" — ولا تحذف السطور القديمة.

---

## آخر حالة معروفة للمشروع

- **الفرع النشط**: `claude/wakalat-bradis-project-7iwyup`
- **Backend**: مكتمل بالكامل (FastAPI + PostgreSQL)، 31 اختبار ناجح،
  Docker + CI جاهزين. فيه الآن rate limiting بسيط على تسجيل الدخول
  (in-memory، 5 محاولات فاشلة = قفل 15 دقيقة)، و`PATCH /api/v1/users/me/password`
  لتغيير كلمة المرور الذاتي. خصم محفظة الوكيل (`wallet_service.deduct_for_order`)
  صار يقفل صف الوكيل (`SELECT ... FOR UPDATE` + `populate_existing`) قبل
  فحص/خصم الرصيد، لمنع تجاوز الرصيد أو الحد الائتماني عند طلبين متزامنين.
  رفع الملفات (`save_private_file`) صار محصَّناً بـ allowlist لنوع
  المحتوى (jpg/png/webp/pdf فقط) وحد أقصى 5MB، وتقديمها عبر `/api/v1/files`
  يفرض Content-Type آمناً من الامتداد المخزَّن بدل تخمين النظام. كل
  حسابات المستخدمين فيها الآن `token_version` — تغيير كلمة المرور يزيده
  فيُبطل فوراً كل توكن JWT قديم في كل الأجهزة، مع إصدار توكن جديد مباشرةً
  حتى لا تنقطع جلسة من نفّذ التغيير. طبقة HTTP فيها ترويسات أمان قياسية
  (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, HSTS).
- **مهم**: `CORS_ORIGINS` صار حقلاً إلزامياً في `.env` (بدون قيمة
  افتراضية) — أي بيئة جديدة (بما فيها CI) لازم تحدّده صراحة وإلا
  التطبيق لا يقلع.
- **مهم**: هناك ترحيل جديد `0002_add_user_token_version` — أي جهاز
  يسحب هذا الكود لازم يشغّل `alembic upgrade head` قبل تشغيل الـ backend
  وإلا ستفشل كل الاستعلامات على جدول users.
- **Frontend**: **مكتمل لكل الشاشات الخمس** — تسجيل الدخول، لوحة
  التحكم، الطلبات، الوكلاء (B2B)، العملات وسعر الصرف، **وحسابي**
  (بيانات شخصية + كرت ملف الوكالة للوكلاء + تغيير كلمة المرور). كلها
  مبنية ومُختبرة يدوياً في المتصفح عبر بيانات حقيقية من الـ API، لأدوار
  مختلفة (admin/agent/customer).
- **آخر commit مدفوع**: انظر أعلى "السجل الزمني" أدناه.

## ملاحظات إعداد بيئة محلية (مهمة لأي جهاز جديد)

- استخدم **Python 3.13** لبيئة الـ backend الافتراضية (venv)، **ليس**
  Python 3.14 — بعض المكتبات المثبَّتة (`pydantic-core`, `psycopg2-binary`)
  ما عندها wheels جاهزة لـ 3.14 بعد، وبيحاول pip يبنيها من المصدر ويفشل
  بدون Visual Studio Build Tools.
- `psycopg2-binary` في `requirements.txt` مثبَّت على `2.9.10` (بدل
  `2.9.9` الأصلي) لأن هذا الإصدار فيه wheel جاهز لـ Windows/cp313.
- إذا كان عندك أكثر من نسخة PostgreSQL على الجهاز، تأكد من الـ port
  الصحيح في `postgresql.conf` قبل ضبط `DATABASE_URL` في `.env`.
- سكربتات Python اللي فيها `print()` بالعربي (زي
  `scripts/seed_initial_data.py`) تحتاج `PYTHONIOENCODING=utf-8` قبل
  التشغيل على ويندوز، وإلا بترمي `UnicodeEncodeError` بسبب ترميز cp1252
  الافتراضي.
- **تحذير مهم لأي جهاز ويندوز**: نظام الملفات على ويندوز لا يفرّق بين
  الأحرف الكبيرة والصغيرة. تجنّب إنشاء ملفين بنفس الاسم يختلفان بحرف
  واحد فقط (Case) في نفس المجلد — هذا سبب باگ حقيقي كسر التطبيق بالكامل
  (`AuthContext.tsx` تعارض مع `authContext.ts`، تم حله بإعادة التسمية
  إلى `authContextValue.ts`).
- **`uvicorn --reload` (WatchFiles) ممكن يفوّت تعديلات على ويندوز**: لو
  عدّلت عدة ملفات Python متتالية وظهرت أخطاء 404/سلوك قديم غير متوقع من
  الـ API رغم إن الكود صحيح، جرّب تعيد تشغيل uvicorn يدوياً (kill
  العمليتين reloader+worker وشغّله من جديد) بدل ما تفترض إن فيه باگ في
  الكود.

## المهمة القادمة المقترحة

كل الشاشات الخمس المخطَّط لها مبنية الآن، وكل توصيات المراجعة الأمنية
السابقة نُفِّذت (رفع/تقديم الملفات، ترويسات الأمان، إبطال الجلسات، قفل
صفوف المحفظة). توصيات أمنية أقل أولوية لسه مفتوحة لو حبيتوا تتناولوها
لاحقاً: (1) `python-jose` مكتبة صيانتها بطيئة، يُفضَّل الانتقال لـ
`PyJWT` مستقبلاً (تحسين صيانة فقط، مو ثغرة مؤكدة حالياً)، (2) لا يوجد
فحص ثغرات تلقائي للاعتماديات (`pip-audit`/`npm audit`) في CI، (3)
التخزين المحلي للمرفقات الحساسة بدون تشفير قرص — مقبول لبيئة تطوير
بمثيل واحد، يحتاج مراجعة قبل أي نشر إنتاجي حقيقي (أو الانتقال لـ S3
كما مصمَّم `storage.py` أصلاً). الخطوات المقترحة التالية: مراجعة/تحسين
الشاشات الحالية حسب ملاحظات استخدام فعلية، أو أي أولوية عمل جديدة من
المستخدم.

---

## السجل الزمني (أضف سطراً جديداً في الأعلى، لا تحذف القديم)

- **2026-07-23 — جلسة سحابية (تحصين رفع الملفات + إبطال الجلسات)**:
  بصفتي مستشاراً تقنياً، راجعت الأمان مرة أخرى بعد سؤال مباشر من
  المستخدم ولقيت سلسلة ثغرات حقيقية قابلة للتسلسل: `save_private_file`
  ما كان يتحقق من نوع/حجم الملف المرفوع، و`/api/v1/files` كان يقدّم
  الملفات بـ Content-Type مخمَّن من نظام التشغيل بدون `nosniff` — يعني
  لو حد رفع ملف `.html`/`.svg` باسم "إشعار دفع" وفتحه موظف مراجعة في
  متصفحه، كان ممكن ينفّذ JS مخزَّن (Stored XSS). وبما إن توكن الدخول
  محفوظ بـ `localStorage` (لا httpOnly cookie)، ده كان يعني سرقة توكن
  الموظف/المدير مباشرة — استحواذ كامل على الحساب لمدة تصل 12 ساعة بدون
  أي طريقة لإبطاله. أصلحت الثلاثة معاً: (1) `storage.py` صار يرفض أي
  ملف مرفوع نوعه خارج jpg/png/webp/pdf أو حجمه أكبر من 5MB، (2)
  `files.py` صار يفرض Content-Type من امتداد الملف المخزَّن (مش تخمين
  النظام)، (3) أضفت middleware ترويسات أمان قياسية في `main.py`
  (X-Content-Type-Options: nosniff, X-Frame-Options: DENY,
  Referrer-Policy, Strict-Transport-Security). كمان لاحظت إن تغيير
  كلمة المرور ما كان يُبطل التوكنات القديمة (JWT عديم الحالة) — فلو
  توكن اتسرق فعلاً، تغيير كلمة المرور ما كان يحميك لمدة تصل 12 ساعة.
  أضفت `token_version` لجدول users (ترحيل `0002_add_user_token_version`،
  **لازم `alembic upgrade head` على أي جهاز جديد**)، يُضمَّن في كل JWT
  كـ claim `tv`، ويُقارَن في `get_current_user`؛ تغيير كلمة المرور يزيده
  فيُبطل كل توكن قديم فوراً، والـ endpoint نفسه يُرجع توكناً جديداً
  ليستمر من غيّر كلمة مروره بدون انقطاع (عدّلت الفرونت‌إند: أضفت
  `refreshToken` لـ `AuthContext` يستدعيه `useChangePassword` بدل
  الاعتماد على `login` الكامل). اختبرت **حياً على PostgreSQL الحقيقي**
  (مو SQLite بس): سجّلت دخول عبر واجهة تسجيل الدخول الفعلية، غيّرت كلمة
  المرور من شاشة "حسابي" الحقيقية، تأكدت إن الجلسة استمرت بدون تسجيل
  خروج قسري، وتأكدت مباشرة عبر fetch إن التوكن القديم يُرفَض تحديداً
  برسالة "انتهت صلاحية هذه الجلسة" (مو خطأ فك تشفير عام) بينما التوكن
  الجديد يعمل. أضفت 5 اختبارات جديدة (`test_storage.py` × 3،
  `test_change_password_increments_token_version`،
  `test_password_change_invalidates_old_token_and_issues_new_one`) —
  **31/31 اختبار ناجح**. `npm run build` نجح بدون أخطاء TypeScript.
  التالي: التوصيات الأقل أولوية أعلاه، أو مراجعة تجربة الاستخدام، أو
  أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (قفل صفوف محفظة الوكيل)**: نفّذت آخر توصية
  مؤجَّلة من مراجعة الأمان: `wallet_service.deduct_for_order` كان يقرأ
  `agent.wallet_balance` من الجلسة بدون قفل، فلو وصل طلبان لنفس الوكيل
  في نفس اللحظة (نفس الرصيد أو الحد الائتماني)، كل واحد يمر فحص الكفاية
  منفرداً ويتجاوز الاثنان معاً الحد المسموح. أضفت
  `db.query(AgentProfile).filter(...).populate_existing().with_for_update().one()`
  قبل الفحص/الخصم — `with_for_update()` بيسوي `SELECT ... FOR UPDATE`
  حقيقي على PostgreSQL (يقفل الصف حتى commit/rollback الجلسة)، وعلى
  SQLite (بيئة الاختبارات) بيتجاهله SQLAlchemy بصمت بدون خطأ، فالاختبارات
  الحالية الأربعة في `test_wallet_service.py` ضلت تعمل بدون تعديل.
  `populate_existing()` ضروري مع `with_for_update()` — بدونه SQLAlchemy
  يرجّع نفس الكائن المخزَّن بالـ identity map بقيمه القديمة بدل قراءة
  الصف الطازج بعد القفل. 26/26 اختبار ناجح (شاملة `test_order_service.py`
  اللي بيغطي مسار إنشاء الطلب كاملاً مع الخصم). **ملاحظة**: التزامن
  الحقيقي غير قابل للاختبار على SQLite (الاختبارات كلها على نفس
  الاتصال)، فالتحقق هنا منطقي/بمراجعة الكود، مو باختبار تزامن فعلي —
  لو حبيتوا تأكيد إضافي، يحتاج اختبار يدوي على PostgreSQL بطلبين
  متزامنين فعلاً. التالي: مراجعة تجربة الاستخدام، أو أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (شاشة حسابي)**: أضفت `PATCH
  /api/v1/users/me/password` لتغيير كلمة المرور الذاتي (ما كان موجوداً
  إطلاقاً في الـ backend) مع تحقق من كلمة المرور القديمة ورفض تكرار
  نفس الكلمة، وبنيت شاشة "حسابي" الكاملة: بيانات شخصية، كرت ملف
  الوكالة B2B (لحسابات agent فقط، من `GET /agents/me`)، وفورم تغيير
  كلمة المرور. اختبرت الاثنين (عميل عادي ووكيل) يدوياً في المتصفح
  بحسابات حقيقية — كل شيء شغال. حذفت `ComingSoonPage.tsx` (صار كود
  ميت بعد اكتمال آخر شاشة). 26/26 اختبار ناجح. **ملاحظة تشغيلية
  مهمة**: واجهت حالة فيها `uvicorn --reload` ما التقط تعديلات على
  `users.py`/`user_service.py` رغم إنها كانت محفوظة على القرص — اضطررت
  أعيد تشغيل السيرفر يدوياً. لو واجهتوا سلوك API قديم غير منطقي، جرّبوا
  هذا أول شي. التالي: مراجعة تجربة الاستخدام الكاملة، أو قفل صفوف
  المحفظة، أو أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (تنفيذ توصيات المراجعة)**: نفّذت أغلب
  التوصيات من مراجعة المستشار التقني السابقة: (1) `app/core/rate_limit.py`
  جديد — قفل 15 دقيقة بعد 5 محاولات دخول فاشلة على نفس الحساب (in-memory،
  يحتاج Redis لو صار النشر multi-instance مستقبلاً)، (2) `CORS_ORIGINS`
  صار إلزامياً في `Settings` (كان افتراضياً `["*"]` بصمت)، (3) منع إنشاء
  أكثر من طلب استرداد pending/approved واحد لنفس الطلب (409)، (4) تعليق
  تحذير بارز أعلى `docker-compose.yml` يوضّح إن أسراره للتطوير المحلي
  فقط. أضفت اختبارين جديدين (23/23 ناجح). لم أنفّذ: قفل صفوف قاعدة
  البيانات لمحفظة الوكلاء (row locking) — مؤجَّل لأن SQLite (المستخدَم
  في الاختبارات) لا يدعم `FOR UPDATE` بنفس سلوك PostgreSQL، يحتاج تصميم
  ومسار اختبار منفصل قبل التنفيذ. التالي: قفل صفوف المحفظة (إن احتجناه
  فعلاً تحت حمل حقيقي)، أو بناء شاشة "حسابي".
- **2026-07-23 — جلسة سحابية (مراجعة تقنية شاملة)**: راجعت الـ backend
  كاملاً بصفة مستشار تقني (أمان، منطق مالي، بنية). لقيت وأصلحت باگين
  ماليين حقيقيين في `refund_service.py`: (1) ما كان فيه سقف يمنع طلب
  استرداد بمبلغ أكبر من قيمة الطلب الأصلية، (2) تنفيذ الاسترداد
  (`process_refund`) كان يغيّر حالة الطلب إلى `refunded` مباشرة بدون
  تسجيل السطر في `order_status_logs`، بما يخالف قاعدة "كل انتقال حالة
  يُسجَّل" المذكورة في README. أضفت اختبارين في `tests/test_refund_service.py`
  (21/21 ناجح الآن). التوصيات الأخرى (rate limiting على تسجيل الدخول،
  تحصين الأسرار الافتراضية في docker-compose.yml قبل أي نشر إنتاجي،
  منع تكرار طلبات استرداد معلَّقة لنفس الطلب) موثّقة لكن لم تُنفَّذ بعد
  — انظر آخر رد للمستشار في المحادثة للتفاصيل الكاملة. التالي: تنفيذ
  التوصيات المتبقية حسب أولويتها، أو بناء شاشة "حسابي".
- **2026-07-23 — جلسة سحابية**: أنشأت بيئة تطوير محلية كاملة (Python
  3.13 + PostgreSQL محلي + venv + قاعدة بيانات + `.env`)، شغّلت
  الـ backend والـ frontend محلياً، وبنيت الشاشات التفصيلية الثلاث
  المتبقية (الطلبات، الوكلاء B2B، العملات وسعر الصرف) بالكامل مع اختبار
  يدوي حقيقي في المتصفح عبر بيانات فعلية من الـ API. اكتشفت وأصلحت
  باگين: (1) تعارض أسماء ملفات حسّاس للأحرف كسر التطبيق بالكامل على
  ويندوز، (2) رصيد محفظة الوكيل ما كان يتحدّث فوراً بعد الإيداع. دفعت
  كل هذا العمل الآن. التالي: بناء شاشة "حسابي"، أو أي توجيه آخر من
  المستخدم.
- **2026-07-23 — جلسة سحابية**: أنشأت هذا الملف. آخر حالة كما موثّقة
  أعلاه. لم أبدأ بعد في الشاشات التفصيلية.
