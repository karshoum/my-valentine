# سجل التقدّم المشترك — وكالة برادايس

> هذا الملف هو نقطة التنسيق بين أي جلسة Claude Code تعمل على هذا
> المشروع (سواء من السحابة أو من جهاز محلي). **كل جلسة يجب أن:**
> 1. تعمل `git pull` وتقرأ هذا الملف **قبل** بدء أي عمل جديد.
> 2. تحدّث القسم أدناه بعد **كل** دفعة عمل منجزة (commit/push)، وتضيف
>    سطراً جديداً في "السجل الزمني" — ولا تحذف السطور القديمة.

---

## آخر حالة معروفة للمشروع

- **الفرع النشط**: `claude/wakalat-paradise-project-7iwyup` (مرّ بثلاثة
  أسماء قديمة قبل ما يستقر نهائياً على هذا الإملاء المطابق للشعار الفعلي:
  `claude/wakalat-bradis-project-7iwyup` ثم
  `claude/wakalat-baradais-project-7iwyup` ثم
  `claude/wakalat-baradise-project-7iwyup` — كلها متروكة على GitHub
  كنسخ قديمة غير مستخدَمة، لكن **كل العمل الجديد يجب أن يذهب للفرع
  الحالي فقط**).
- **مهمة لأي جلسة/جهاز آخر (خصوصاً الجهاز المحلي)**: إذا كان عندك
  Checkout محلي على أي من الاسمين القديمين أعلاه، نفّذ:
  ```bash
  git fetch origin
  git checkout -B claude/wakalat-paradise-project-7iwyup origin/claude/wakalat-paradise-project-7iwyup
  ```
  وتابع العمل من الفرع الحالي فقط من الآن فصاعداً. لا تدفع أي شي جديد
  على أي اسم فرع قديم.
- **JWT**: تمت الترقية من `python-jose` إلى `PyJWT==2.9.0` بالكامل
  (`app/core/security.py` فقط تأثّر — `create_access_token`/`decode_access_token`).
  لا أثر لـ `jose` في أي مكان آخر بالكود.
- **كتالوج خدمات الوكالة الفعلي**: أُضيف عبر `scripts/seed_agency_services.py`
  (بيانات الخدمات في `scripts/agency_services_catalog.py`) — **39 خدمة
  حقيقية** بالضبط كما أرسلتها الوكالة، موزّعة على 11 تصنيفاً (أُضيف 7
  تصنيفات جديدة لـ `ServiceCategory`: `ship_ticket`, `renewal_extension`,
  `security_approval`, `procedure_package`, `tourism_package`,
  `document_extraction`, `attestation`). **مهم جداً**: كل خدمة أُدخلت
  بسعر أساسي مؤقت **0.00 دولار** لأن الوكالة لم ترسل بعد الأسعار
  الفعلية — يجب على الإدارة تحديث كل سعر يدوياً عبر
  `PATCH /api/v1/services/{id}` (أو من شاشة لوحة التحكم إن وُجدت لاحقاً)
  قبل عرض أي خدمة فعلياً للعملاء. السكربت idempotent (آمن التشغيل أكثر
  من مرة، يتجاوز أي عنوان موجود مسبقاً). خدمتا "إقامة رومانيا الدائمة"
  و"إقامة بولندا الدائمة" **لم تُضافا عمداً** لأن الوكالة وصفتهما بـ"قريباً".
- **مهم**: ترحيل جديد `0003_expand_service_category` — أي جهاز
  يسحب هذا الكود لازم يشغّل `alembic upgrade head` (PostgreSQL لا يدعم
  حذف قيمة Enum، فـ downgrade لهذا الترحيل غير قابل للتنفيذ فعلياً — راجع
  تعليقات الملف).
- **🔴 إصلاح حرج لاحق (اكتُشف أثناء أول نشر حقيقي على PostgreSQL)**:
  معرّف هذا الترحيل كان أصلاً `0003_expand_service_category_enum` (33
  حرف) — أطول من الحد الافتراضي لعمود `alembic_version.version_num` في
  Alembic (`VARCHAR(32)`). SQLite (المستخدَم في كل اختبارات pytest
  المحلية) لا يفرض حدود طول الأعمدة إطلاقاً، فما ظهر الخطأ إلا أول مرة
  شغّلنا `alembic upgrade head` على PostgreSQL حقيقي (أثناء نشر Railway)،
  برسالة `psycopg2.errors.StringDataRightTruncation`. تم تقصير المعرّف
  إلى `0003_expand_service_category` (29 حرف) في كل من الملف نفسه (أُعيدت
  تسميته أيضاً) و`down_revision` في `0004_add_service_discount_fields.py`.
  **مهم جداً لأي بيئة (جهاز محلي أو خادم) طبّقت `alembic upgrade head`
  قبل هذا الإصلاح ووصلت فعلياً لهذا الترحيل على PostgreSQL حقيقي** (وليس
  SQLite): جدول `alembic_version` عندها لسه فيه المعرّف القديم، ولازم
  تشغّل يدوياً قبل أي `alembic upgrade head` جديد:
  ```sql
  UPDATE alembic_version SET version_num = '0003_expand_service_category'
  WHERE version_num = '0003_expand_service_category_enum';
  ```
  (لو ما وصلتوا أصلاً لهذا الترحيل بعد، أو كانت بيئتكم SQLite، لا داعي
  لأي شي — `alembic upgrade head` العادي كافٍ).
- **إدارة الخدمات مكتملة الآن (Backend + Frontend)**: كانت ناقصة (تعديل
  فقط، بلا حذف ولا عروض خصم، وبلا شاشة إدارة أصلاً) — تم إكمالها بالكامل:
  - `DELETE /api/v1/services/{id}` (admin فقط) — يرفض الحذف بـ 409 إذا
    كانت الخدمة مرتبطة بأي طلب سابق (حفاظاً على سجل الطلبات)، ويقترح
    التعطيل (`is_active=false`) بدلاً من ذلك.
  - عروض خصم محدودة المدة: حقلا `discount_percentage`/`discount_valid_until`
    على `Service` (ترحيل `0004_add_service_discount_fields`)، خاصية
    `effective_price_usd` محسوبة، ومسار `PATCH /api/v1/services/{id}/discount`
    (admin فقط؛ يجب إرسال الحقلين معاً لتفعيل عرض أو كليهما null لإلغائه).
  - `GET /api/v1/services/manage/all` (staff) يُعيد كل الخدمات (مفعَّلة
    وغير مفعَّلة) لأغراض الإدارة.
  - شاشة "الخدمات" الكاملة في الـ Frontend (`frontend/src/features/services/`):
    جدول بفلاتر تصنيف، إضافة/تعديل/حذف/عرض خصم، مع تقييد زري الخصم
    والحذف على حساب admin فقط في الواجهة (مطابقةً لقيود الـ backend).
    اختُبرت حياً بالكامل عبر متصفح حقيقي (Playwright): إنشاء، تعديل سعر،
    تفعيل خصم 25% على "تأشيرة تركيا"، حذف خدمة تجريبية بلا طلبات، ومحاولة
    حذف خدمة **لها طلب فعلي** فرُفضت بـ 409 كما هو متوقع.
  - أضافت `tests/test_service_service.py` (6 اختبارات: حذف محظور/مسموح،
    حساب السعر الفعلي، انتهاء صلاحية الخصم، إلغاء الخصم).
- **دورة حياة الطلب الكاملة مكتملة الآن (Backend + Frontend)**: كانت
  الفجوة الحقيقية إن ما فيه أي شاشة لإنشاء طلب، رفع دفع، مراجعته، أو
  إيصال المستند النهائي — رغم جهوزية كل الـ API. أُكملت الأربعة، مع
  مراعاة تفصيل مهم من صاحب الوكالة: **الموظف عادة يحجز فعلياً من موقع
  خارجي (ناقل/سفارة) ثم يرجع يرفع التذكرة/الفيزا الصادرة هنا** — هذا
  يطابق تماماً حالة `in_system` الموجودة أصلاً في `ALLOWED_TRANSITIONS`
  (`order_service.py`):
  - `POST /api/v1/orders/{id}/deliverable` (staff) يرفع ويربط المستند
    النهائي، مرفوض إلا إذا كانت حالة الطلب `in_system` أو `completed`
    على الأقل. حقل `Order.deliverable_file_url` جديد (ترحيل
    `0005_add_order_deliverable_file`) مع خاصية `deliverable_signed_url`
    محسوبة (رابط موقّت زي رابط إيصال الدفع).
  - `PaymentOut` صار يُرجع `receipt_signed_url` (رابط موقّت لصورة
    إشعار الدفع — **كان غائباً تماماً قبل كذا**، يعني الموظف ما كان
    يقدر يشوف الإيصال أصلاً وقت المراجعة!) و`order_number` (لعرضه في
    شاشة المراجعة).
  - `GET /api/v1/payments` (staff) جديد — طابور عمل المراجعة، بتصفية
    اختيارية حسب الحالة.
  - Frontend: زر "طلب جديد" في شاشة الطلبات (اختيار خدمة + عملة +
    مسافرين)، نموذج رفع إثبات الدفع (multipart، صورة اختيارية لفيزا
    وإلزامية لبنكك) داخل لوحة تفاصيل الطلب، شاشة "المدفوعات" الجديدة
    (طابور مراجعة + معاينة الإيصال + اعتماد/رفض)، ونموذج رفع المستند
    النهائي (يظهر للموظف فقط عند وصول الطلب لحالة `in_system`) + رابط
    تحميل يظهر للجميع (شاملاً العميل) بمجرد رفعه.
  - **مهم**: أضفت `buildFileUrl()` في `frontend/src/lib/apiClient.ts` —
    الروابط الموقّتة القادمة من الـ backend مسارات نسبية (`/api/v1/files/...`)،
    ولازم تُدمَج مع عنوان الـ backend الفعلي قبل استخدامها في `<img>`/`<a>`
    لأن الفرونت‌إند والخلفية على منفذين مختلفين — بدونها الروابط تحاول
    تجيب الملف من عنوان الفرونت‌إند نفسه وتفشل بصمت.
  - اختُبرت الدورة الكاملة حياً بمتصفح Playwright حقيقي من البداية
    للنهاية: عميل ينشئ طلباً ويرفع إثبات دفع → مدير يراجع ويعتمد الدفع
    (الطلب ينتقل تلقائياً لـ processing) → مدير ينقل الطلب لـ in_system
    → مدير يرفع تذكرة PDF وهمية → مدير ينقل الطلب لـ completed → رجعت
    سجّلت دخول كالعميل نفسه وتأكدت إنه يشوف حالة "مكتمل" ورابط تحميل
    فعلي يرجّع PDF حقيقي بحالة 200.
  - أضفت `tests/test_order_deliverable_service.py` (4 اختبارات) و
    `tests/test_payment_service.py` (4 اختبارات) — 50/50 اختبار ناجح.
- **شاشة المستردات (Refunds) مكتملة الآن (Backend + Frontend)**: الـ
  backend كان جاهزاً بالكامل (`refund_service.py`: إنشاء/اعتماد/رفض/تنفيذ)
  لكن بلا أي واجهة ولا حتى مسار GET لعرض القائمة. أضفت `GET /api/v1/refunds`
  (staff، تصفية اختيارية حسب الحالة) وخاصية `Refund.order_number` محسوبة
  (زي `Payment.order_number`)، وأضفت `refunds: list[RefundOut]` لـ
  `OrderOut` عشان العميل يشوف حالة طلب استرداده مباشرة من تفاصيل طلبه.
  Frontend: نموذج "طلب استرداد" داخل لوحة تفاصيل الطلب (يظهر فقط لو
  الطلب مؤهَّل — processing/in_system/completed — وما فيه طلب استرداد
  مفتوح بالفعل)، وشاشة "المستردات" الجديدة (طابور مراجعة بفلاتر حالة)
  مع نافذة مراجعة تعكس دورة العمل ذات الخطوتين المتعمَّدة في الـ backend:
  أولاً اعتماد/رفض، ثم زر "تنفيذ الاسترداد" منفصل يظهر بعد الاعتماد فقط
  (التنفيذ هو اللي فعلياً ينقل الطلب لحالة refunded ويرجّع المبلغ لمحفظة
  الوكيل إن كان صاحب الطلب وكيلاً). زرا الاعتماد/الرفض/التنفيذ محصورون
  بحساب admin فقط في الواجهة (يطابق `require_admin` بالـ backend — دورها
  أعلى صرامة من المدفوعات اللي يكفيها `require_staff`). اختُبرت الدورة
  كاملة حياً بمتصفح Playwright حقيقي: عميل أنشأ طلباً ورفع دفعاً، مدير
  اعتمد الدفع، عميل قدّم طلب استرداد، مدير اعتمده ثم نفّذه، وتأكدت إن
  الطلب انتقل فعلياً لحالة "مسترجَع" (نهائية، بلا انتقالات أخرى) وإن
  سجل الاسترداد يظهر بحالة "منفَّذ" داخل تفاصيل الطلب. أضفت اختبارين
  جديدين في `tests/test_refund_service.py` — **52/52 اختبار ناجح**.
- **Backend**: مكتمل بالكامل (FastAPI + PostgreSQL)، 52 اختبار ناجح،
  Docker + CI جاهزين. فيه الآن rate limiting بسيط على تسجيل الدخول
  (in-memory، 5 محاولات فاشلة = قفل 15 دقيقة)، و`PATCH /api/v1/users/me/password`
  لتغيير كلمة المرور الذاتي. خصم محفظة الوكيل (`wallet_service.deduct_for_order`)
  صار يقفل صف الوكيل (`SELECT ... FOR UPDATE` + `populate_existing`) قبل
  فحص/خصم الرصيد، لمنع تجاوز الرصيد أو الحد الائتماني عند طلبين متزامنين.
  رفع الملفات (`save_private_file`) صار محصَّناً بـ allowlist لنوع
  المحتوى (jpg/png/webp/pdf فقط) وحد أقصى 5MB، وتقديمها عبر `/api/v1/files`
  يفرض Content-Type آمناً من الامتداد المخزَّن بدل تخمين النظام. كل
  حسابات المستخدمين فيها الآن `token_version` — تغيير كلمة المرور يزيده
  فيُبطل فوراً كل توكن JWT قديم في كل الأجهزة، مع إصدار توكن جديد مباشرةً
  حتى لا تنقطع جلسة من نفّذ التغيير. طبقة HTTP فيها ترويسات أمان قياسية
  (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, HSTS).
- **مهم**: `CORS_ORIGINS` صار حقلاً إلزامياً في `.env` (بدون قيمة
  افتراضية) — أي بيئة جديدة (بما فيها CI) لازم تحدّده صراحة وإلا
  التطبيق لا يقلع.
- **مهم**: هناك ترحيل جديد `0002_add_user_token_version` — أي جهاز
  يسحب هذا الكود لازم يشغّل `alembic upgrade head` قبل تشغيل الـ backend
  وإلا ستفشل كل الاستعلامات على جدول users.
- **Frontend**: **مكتمل لكل الشاشات الثمانية** — تسجيل الدخول، لوحة
  التحكم، الطلبات (شاملاً إنشاء طلب جديد ورفع إثبات الدفع)، الوكلاء
  (B2B)، المدفوعات (طابور مراجعة يدوية للموظف/المدير)، **المستردات**
  (طابور اعتماد/رفض/تنفيذ للمدير)، الخدمات (كتالوج كامل: إضافة/تعديل/
  حذف/عروض خصم)، العملات وسعر الصرف، وحسابي (بيانات شخصية + كرت ملف
  الوكالة للوكلاء + تغيير كلمة المرور). كلها مبنية ومُختبرة حياً في
  المتصفح عبر بيانات حقيقية من الـ API، لأدوار مختلفة (admin/agent/customer)،
  بما يشمل اختبار دورة حياة طلب كاملة من الإنشاء وحتى الاسترداد (انظر أعلاه).
- **جديد (طلب مستخدم مباشر — أولوية الهاتف + قوقل + إيميل + متطلبات المستندات)**:
  - **Mobile-first**: القائمة الجانبية (`Sidebar.tsx`) صارت drawer منسدل
    على الهاتف (`md:hidden`, يُفتح من زر ☰ جديد في `Topbar.tsx`، يُغلق
    بالخلفية أو بالنقر على رابط)، بدل ما كانت تختفي تماماً بلا أي بديل
    تنقّل على الشاشات الصغيرة قبل هذا الإصلاح. أضفت `flex-wrap` لصفوف
    عناوين الشاشات (خدمات/طلبات/وكلاء/عملات) و`p-4 sm:p-6` للمحتوى
    الرئيسي. اختُبر حياً بمتصفح Playwright على viewport هاتف حقيقي
    (390×844) — صفر overflow أفقي على كل الشاشات المفحوصة.
  - **تسجيل دخول عبر قوقل**: `POST /api/v1/auth/google` جديد (يتحقق من
    Google ID Token عبر مكتبة `google-auth`، يربط حساب موجود بنفس
    البريد أو ينشئ عميلاً جديداً). `users.phone`/`password_hash` صارا
    اختياريين + عمود `google_id` جديد (ترحيل
    `0007_add_google_login_support`، **لازم `alembic upgrade head`**).
    `GOOGLE_CLIENT_ID`/`VITE_GOOGLE_CLIENT_ID` فارغَين حتى الآن عمداً
    (لا نطاق فعلي بعد) — زر "الدخول عبر قوقل" في `LoginPage.tsx` لا
    يظهر إلا إذا ضُبط `VITE_GOOGLE_CLIENT_ID`. **مهم**: أصلحت أثناء هذا
    العمل ثغرة حقيقية كانت لتظهر لاحقاً: `auth_service.authenticate`
    كان سيرمي استثناء غير معالَج (500) لو حاول مستخدم Google-only
    (بلا `password_hash`) تسجيل الدخول التقليدي ببريده.
  - **إشعارات إيميل**: `app/services/email_service.py` جديد (SMTP عام
    عبر `smtplib`، فارغ الإعدادات = تجاوز صامت، فشل الاتصال لا يوقف
    إنشاء/تحديث الطلب أبداً — مغلَّف بالكامل بـ try/except). يُستدعى من
    `order_service.create_order` (تأكيد استلام) و`update_order_status`
    (تحديث حالة)، لذا التحقق اليدوي من الدفع أيضاً يُطلق الإيميل تلقائياً
    (يمر عبر نفس الدالة). `SMTP_HOST`/`SMTP_FROM_EMAIL`/... فارغة حتى
    الآن عمداً بانتظار حساب SMTP فعلي.
  - **متطلبات مستندات كل خدمة**: جدول `service_requirements` جديد
    (ترحيل `0006_add_service_requirements`) — بنود نصية مرتّبة لكل
    خدمة، تظهر تلقائياً في `ServiceOut.requirements` وتُعرض للعميل في
    نموذج "طلب جديد" فور اختيار الخدمة. الإدارة تعدّلها في أي وقت من
    شاشة "الخدمات" (أيقونة 📋 جديدة بجانب كل خدمة، لكل الموظفين وليس
    admin فقط — تطابق `require_staff` بالـ backend). زرعت
    `scripts/seed_service_requirements.py` بمحتوى متطلبات فعلي مطابق
    تماماً لنص الوكالة لكل الـ 39 خدمة (تذاكر، فيزات دول محدَّدة، فيزات
    إفريقية عامة، إقامات، تجديد/تمديد، موافقات أمنية، بكجات تخليص
    إجراءات، بكجات سياحية، استخراج مستندات، توثيق).
  - أضفت 3 ملفات اختبار جديدة (`test_service_requirement_service.py`،
    `test_google_login.py`، `test_email_service.py`) — **69/69 اختبار
    ناجح**. `npm run build`/`oxlint`/`tsc --noEmit` نظيفة.
- **جديد (طلب مستخدم مباشر — بحث حجوزات طيران حقيقي كحل لأهم فجوة
  ذُكرت له صراحةً عند مراجعة المشروع)**:
  - المستخدم سأل صراحةً هل النظام يعرض أسعار/رحلات طيران حقيقية (مثال:
    الدمام-إسطنبول) — الجواب كان لا، كانت خدمة "تذاكر طيران" سطراً
    عاماً واحداً بلا حقل مسار/تاريخ إطلاقاً. طلب حل المشكلة بنفس فكرة
    مواقع الحجز (كل الخطوط وكل الرحلات المتاحة بأسعارها)، مع تحكّم
    المدير برسم حجز (فلات أو نسبة، يختار لكل حالة)، ورقم واتساب إلزامي
    من العميل لسهولة التواصل. أكّد أن الوكالة **لا تملك اشتراك GDS
    حالياً**، فاخترنا معاً Amadeus for Developers (تسجيل مجاني فوري،
    بيئة Test تعمل الآن بدون موافقة عمل، Production لاحقاً)، ووافق أن
    **الحجز الفعلي (إصدار التذكرة) يبقى يدوياً** كما هو معمول به (بحث
    فقط، لا حجز/دفع آلي عبر الـ API).
  - **Backend**: `app/integrations/amadeus_client.py` (توكن OAuth2
    مخزَّن بالذاكرة + بحث Flight Offers Search، يرفض بوضوح 503 قبل ضبط
    `AMADEUS_API_KEY`/`AMADEUS_API_SECRET` الفارغَين حالياً عمداً).
    `flight_booking_fee_settings` جدول إعداد وحيد (فلات/نسبة، يعدّله
    المدير في أي وقت عبر `PATCH /api/v1/flight-bookings/fee-setting`،
    admin فقط). `flight_booking_details` جدول تفاصيل الرحلة المختارة
    لكل طلب (مسار، تواريخ، شركة الطيران، السعر الحقيقي، والرسم
    المُضاف). `orders.contact_whatsapp` عمود جديد **إلزامي على مستوى
    الـ API لكل الطلبات** (ليس فقط حجوزات الطيران). ترحيل
    `0008_add_flight_booking_tables` (**لازم `alembic upgrade head`**).
    `order_service.create_order` يرفض أي طلب لخدمة تصنيف
    flight/ship_ticket بلا `flight_booking` مرفَق (والعكس)، ويحسب
    `total_amount` من `base_fare_usd + الرسم` بدل سعر الخدمة الثابت.
  - **Frontend**: `FlightSearchSection.tsx` (نموذج بحث + نتائج بأسعار
    شاملة الرسم) مدموج داخل `NewOrderModal.tsx` تلقائياً عندما تكون
    الخدمة المختارة من تصنيف تذاكر طيران/بواخر، مع `SelectedFlightCard.tsx`
    لعرض/تغيير الرحلة المختارة. حقل رقم واتساب صار إلزامياً في نموذج
    "طلب جديد" لكل الخدمات. شاشة "رسوم الحجز" جديدة (admin فقط) تختار
    بين فلات/نسبة وتحفظ فوراً.
  - أضفت 3 ملفات اختبار جديدة (`test_flight_booking_service.py`،
    `test_flight_search_service.py` بـ Amadeus مُحاكى بالكامل عبر
    monkeypatch، `test_order_flight_booking.py`) — **81/81 اختبار
    ناجح**. غيّرت `sample_service` (فيكستشر مشترك) من تصنيف `flight`
    إلى `insurance` لأن معظم اختبارات الطلبات القديمة لا علاقة لها
    بحجوزات الطيران تحديداً. اختُبر حياً: بحث بلا مفاتيح Amadeus يرجع
    503 برسالة عربية واضحة (Backend وFrontend كلاهما)، `PATCH
    /fee-setting` يعمل (فلات ثم نسبة)، وإنشاء طلب فعلي عبر API ببيانات
    رحلة يدوية (محاكية لما سيرسله البحث الحقيقي) أعطى `total_amount`
    صحيحاً بالضبط (340 + 15 رسم = 355).
- **مهم — تصحيح فوري**: أثناء متابعة المستخدم لخطوات تسجيل Amadeus،
  اكتشفنا أن **Amadeus for Developers أوقف باب التسجيل الذاتي المجاني
  نهائياً بتاريخ 17 يوليو 2026** (بيئة Enterprise API فقط متبقية، تتطلب
  تواصل مبيعات). استبدلنا التكامل بالكامل بـ **Duffel** (تسجيل ذاتي
  مجاني فوري لسه شغّال، مفتاح `duffel_test_...`). التغيير: حذف
  `app/integrations/amadeus_client.py`، أضيف `duffel_client.py`
  (`POST /air/offer_requests?return_offers=true`، Bearer auth +
  ترويسة `Duffel-Version`)، `flight_search_service.py` أُعيد كتابته
  ليطابق شكل استجابة Duffel (`data.offers[].slices[].segments[]`)، مع
  **استبعاد أي عرض غير مُسعَّر بالدولار صراحة** (Duffel قد يُسعّر
  العروض بعملات مختلفة حسب الناقل/المسار، والنظام الداخلي يفترض
  `base_fare_usd` بالدولار حصراً). `config.py`/`.env.example` صارا
  `DUFFEL_API_KEY`/`DUFFEL_BASE_URL`/`DUFFEL_VERSION` بدل متغيرات
  Amadeus. **مهم**: بيئة الترميز نفسها (sandbox) محجوبة عن الإنترنت
  العام إلا نطاقات محدودة (npm/pypi/إلخ)، فما قدرنا نختبر استجابة
  Duffel الحقيقية مباشرة من هذه الجلسة (403 من بروكسي الجلسة، سياسة
  متعمَّدة مو خطأ) — الكود مبني على توثيق Duffel المعروف والمستقر، لكن
  **لازم اختبار بحث حقيقي واحد بعد النشر الفعلي** (على Render أو أي
  خادم عادي بدون هذا القيد) للتأكد من تطابق أسماء الحقول تماماً؛ أي
  اختلاف بسيط تعديله دقيقتين. حدّثت `tests/test_flight_search_service.py`
  بثلاث اختبارات جديدة (شكل Duffel، استبعاد العملات غير الدولار، قائمة
  فارغة) — **82/82 اختبار ناجح**. المستخدم أعطى مفتاح `duffel_test`
  الفعلي في المحادثة مباشرة (لم يُحفَظ في أي ملف مُدفَّع للـ git، فقط
  للاستخدام المحلي في `.env` لاحقاً من قبل المستخدم نفسه).
- **جديد (طلب مستخدم مباشر — الهوية البصرية الرسمية + الشعار الفعلي)**:
  المستخدم رفع صورة الشعار الرسمي (كحلي + ذهبي على خلفية كريمية، شعار
  حرف P متشابك مع طائرة/نخلة، نص "PARADISE Travel & Tourism" بالإنجليزية
  و"برادايس" بالعربية) وطلب إضافته في كل مكان ممكن (شاشات + إيميلات)
  واعتماد ألوانه كهوية بصرية موحّدة للموقع كله.
  - استخرجت الألوان الفعلية من بيكسلات الصورة برمجياً (لا تخمين): كحلي
    `#22364e`، ذهبي يتراوح `#96723d`–`#c9aa73`، كريمي `#f4f2e5`. أضفتها
    كمقياس ألوان Tailwind v4 كامل (`navy-50..900`, `gold-50..800`,
    `cream-50..200`) في `frontend/src/index.css`.
  - استبدلت "emerald" (كان يُستخدَم كلون العلامة التجارية الأساسي في كل
    الأزرار/الحالات النشطة/التركيز) بـ "navy" ميكانيكياً عبر 31 ملف
    frontend، مع لمسات ذهبية مقصودة: حلقة تركيز الحقول (navy + gold)،
    حد ذهبي رفيع على عنصر التنقّل النشط في القائمة الجانبية، وحالة
    الطلب "مكتمل" صارت ذهبية (بدل الكحلي) لتمييزها بصرياً عن "في
    السيستم" (أزرق).
  - الشعار الفعلي (`frontend/public/logo.png`، بحجم محسَّن ~150KB
    ونسخة favicon 64px) صار ظاهراً في: القائمة الجانبية (كمبيوتر
    وهاتف)، شاشة تسجيل الدخول، وأيقونة تبويب المتصفح.
  - **الإيميلات**: أعدت بناء `email_service.py` بالكامل لترسل HTML
    (بدل نص عادي) مع الشعار مُرفَقاً inline عبر Content-ID (يعمل حتى
    بدون استضافة عامة للصورة)، فصلت قالب HTML الموحّد في ملف جديد
    `email_templates.py` (كحلي أعلى + كريمي وسط + ذهبي أسفل، بنفس هوية
    الموقع)، مع نسخة نصية بديلة تلقائياً لعملاء البريد التي لا تدعم HTML.
  - **لم أجد** أي شاشة "تقارير" فعلية في المشروع حالياً (بحثت في الكود
    الكامل) — الميزة غير موجودة بعد لأضيف لها الشعار، فقط ذكرتها هنا
    كملاحظة إذا احتجناها مستقبلاً.
  - **تحديث لاحق (نفس الجلسة)**: الشعار الفعلي يكتب بالإنجليزية
    "PARADISE" (بحرف P)، أكّد المستخدم صراحةً استخدام هذه التسمية.
    استُبدلت كل المتغيرات التقنية الإنجليزية (APP_NAME، بريد المدير
    الافتراضي، مفاتيح localStorage، logger، اسم فرع git) من
    Baradise/baradise إلى Paradise/paradise في نفس الدفعة أدناه. الاسم
    الإنجليزي التقني استقر أخيراً على: bradis (الأصلي) →
    baradais → baradise → **paradise (نهائي، مطابق للشعار)**. الاسم
    العربي "برادايس" ثابت ولم يتغيّر طوال هذه السلسلة.
  - اختبرت حياً بمتصفح Playwright حقيقي: تسجيل الدخول (الشعار + الخلفية
    الكريمية + الزر الكحلي)، لوحة التحكم/الطلبات/الخدمات (القائمة
    الجانبية بالشعار والحد الذهبي على العنصر النشط)، وقالب الإيميل HTML
    (عرضته كصفحة محلية وأخذت لقطة شاشة — يطابق الهوية تماماً). حدّثت
    `tests/test_email_service.py` (بنية MIME تغيّرت من نص عادي لـ
    multipart/related، أضفت اختبار تحميل الشعار) — **83/83 اختبار
    ناجح**. `npm run build`/`oxlint`/`tsc --noEmit` نظيفة.
- **🔴 إصلاح حرج آخر (نشر Railway، بعد إصلاح 0003) — مرَّ بمحاولتين**:
  ترحيل `0008_add_flight_booking_tables` كان يفشل بـ
  `psycopg2.errors.DuplicateObject: type "flight_booking_fee_type" already exists`
  أثناء `alembic upgrade head` على PostgreSQL حقيقي.
  - **المحاولة الأولى** (غير كافية): افترضت إن السبب الوحيد كان تعريف
    `flight_booking_fee_type = sa.Enum(...)` بلا `create_type=False` —
    فـ `op.create_table()` كان يحاول ينشئ النوع تلقائياً مرة ثانية بعد
    الإنشاء اليدوي (`.create(checkfirst=True)`). أضفت `create_type=False`
    فقط. **لكن نفس الخطأ تكرر حرفياً بعد الدفع** — يعني `checkfirst=True`
    في `.create()` اليدوي نفسه ما كان يتجاوز النوع الموجود فعلياً (على
    الأرجح لأن محاولة نشر فاشلة سابقة أنشأت النوع فعلياً، وبقي موجوداً
    في قاعدة Railway الحقيقية رغم رجوع باقي الترحيل بالكامل — سلوك غريب
    يستحق فهماً أعمق لاحقاً لو تكرر بنمط مشابه في ترحيل آخر، لكن غير
    حرج الآن بعد الإصلاح الثاني).
  - **المحاولة الثانية (الحل الفعلي المطبَّق الآن)**: استبدلت
    `.create(checkfirst=True)` بأمر SQL خام مضمون النتيجة بغض النظر عن
    سلوك SQLAlchemy الداخلي:
    ```sql
    DO $$ BEGIN
        CREATE TYPE flight_booking_fee_type AS ENUM ('flat', 'percentage');
    EXCEPTION
        WHEN duplicate_object THEN null;
    END $$;
    ```
    (ونفس المبدأ بالعكس بـ`downgrade()`: `DROP TYPE IF EXISTS ...` بدل
    `.drop(checkfirst=True)`). هذا يضمن نجاح الترحيل سواء كان النوع
    موجوداً مسبقاً من محاولة فاشلة قديمة أو لا، بلا أي اعتماد على منطق
    checkfirst الداخلي لـ SQLAlchemy. `create_type=False` من المحاولة
    الأولى بقي أيضاً (صحيح ومطلوب). تحققت أن الـ 83 اختبار المحلي لا
    زالت ناجحة. **لا حاجة لأي تصحيح يدوي بقاعدة بيانات Railway** — هذا
    الإصلاح يتعامل مع الحالتين (النوع موجود أو غير موجود) تلقائياً.
    إذا فشل نفس الترحيل مرة ثالثة بخطأ مختلف بعد هذا الإصلاح، تأكدوا
    أولاً إن Railway فعلاً يبني من آخر commit مدفوع (تحقق من رقم الـ
    commit بتبويب Deployments) قبل افتراض وجود باگ آخر بالكود.
  - **🟢 السبب الجذري الحقيقي (اكتُشف لاحقاً، أثناء إعداد بيئة محلية
    فعلية بجلسة سحابية أخرى)**: الشك بـ Railway (يبني كود قديم) كان
    خاطئاً بالكامل. أعدنا إنتاج نفس الخطأ **محلياً على قاعدة PostgreSQL
    فاضية 100% أُنشئت للتو** — يعني الباگ حتمي ويحصل من أول محاولة على
    أي قاعدة بيانات، بغض النظر عن أي "بقايا" من محاولات فاشلة قديمة. سبب
    فشل المحاولة الأولى (`create_type=False`): استُخدم `sa.Enum(...)`
    (النوع العام متعدد قواعد البيانات) بدل `postgresql.ENUM(...)` (نوع
    خاص بـ PostgreSQL). معامل `create_type` **غير مفعَّل فعلياً إلا في
    `postgresql.ENUM`** — تمريره لـ `sa.Enum` العام يُقبَل بصمت بلا أي
    تأثير حقيقي على DDL، فـ `op.create_table()` استمر يصدر `CREATE TYPE`
    تلقائياً رغم وجود `create_type=False` في الكود! هذا بالضبط الفرق بين
    0008 (فشلت) و`0001_initial_schema.py` (نجحت من البداية) — 0001
    تستخدم `postgresql.ENUM(..., create_type=False)` بشكل صحيح لكل
    أنواعها. صُحح 0008 لاستيراد `from sqlalchemy.dialects import
    postgresql` واستخدام `postgresql.ENUM(...)` بدل `sa.Enum(...)`
    (مع الإبقاء على حل الـ DO block من المحاولة الثانية كطبقة حماية
    إضافية). تحققت محلياً: مسحت قاعدة PostgreSQL بالكامل وأعدت إنشاءها
    فاضية، وشغّلت `alembic upgrade head` من الصفر — **نجحت لأول مرة
    بلا أي خطأ**، ثم أعدتها مرة ثانية (idempotent) بلا مشاكل. 83/83
    اختبار محلي لا زال ناجح. **هذا هو الإصلاح النهائي المؤكَّد فعلياً
    بقاعدة بيانات حقيقية** — أي محاولة نشر مستقبلية (على Railway، Zeabur،
    أو أي منصة) ستنجح الآن من أول مرة.
- **🔴 تغيير خطة النشر: هجرنا Railway إلى Zeabur (zeabur.com)**: بعد
  محاولتين فشلتا بنفس خطأ `DuplicateObject` على Railway ولاحظنا إن
  اللوگ يوضّح نفس الترحيلات 0001-0007 تنجح من الصفر بكل محاولة (دليل إن
  القاعدة تُعاد لحالة فاضية تلقائياً بعد كل فشل — Alembic يلف كل شي
  بمعاملة واحدة)، ما قدرنا نتأكد نهائياً هل Railway فعلاً كان يبني آخر
  commit مدفوع أو نسخة قديمة عالقة (واجهته المعقدة صعّبت التأكد من
  رقم الـ commit). المستخدم قرّر ترك Railway بعد التجربتين وجرّب
  `adaptable.io` أولاً — **تبيّن إنها مقفولة فعلياً (النطاق ما بيتحل
  DNS، لا علاقة لنا بأي إعداد)** — فانتقلنا لـ **Zeabur** (نطاقها حي
  ويرد 403 على الزيارة الآلية بسبب حماية بوتات، مو لأنها مقفولة). Zeabur
  تدعم النشر من `Dockerfile` مباشرة + PostgreSQL مُدارة، بخطة تجريبية
  برصيد مجاني. **تعديل مهم بالكود بهذه المناسبة**: أضفت `entrypoint.sh`
  جديد في جذر المشروع يشغّل `alembic upgrade head` تلقائياً ثم يبدأ
  `uvicorn` (يقرأ `$PORT` من البيئة إن وُجد، افتراضياً 8000) — وحدّثت
  `Dockerfile` ليستخدمه كـ `CMD` بدل تشغيل `uvicorn` مباشرة. **السبب**:
  على Railway كان الترحيل التلقائي (`alembic upgrade head`) يعتمد على
  إعداد "Start Command" مخصّص يدوي في واجهة Railway نفسها (غير موجود
  بأي ملف بالمستودع) — لو انتقلنا لمنصة جديدة بدون توثيق هذا، بيفشل
  التطبيق بصمت لأنه يحاول يشتغل بلا جداول قاعدة بيانات. الآن الترحيل
  جزء من صورة الـ Docker نفسها (idempotent، آمن التكرار)، فيشتغل صح
  على أي منصة تدعم Dockerfile (Zeabur، Railway، Fly.io...) بلا أي إعداد
  يدوي إضافي. `docker-compose.yml` المحلي غير متأثر (عنده `command:`
  خاص به يطغى على CMD أصلاً). تحققت من 83/83 اختبار محلي ناجح بعد
  التعديل. التالي: تسجيل حساب Zeabur، ربط GitHub repo والفرع، إضافة
  PostgreSQL، ضبط متغيرات البيئة (نفس قائمة `.env.example`)، والنشر.
- **🔴 تسليم للجهاز المحلي — حالة حساب Railway غير واضحة، تحتاج
  تحقيق مباشر**: المستخدم أعلن إنه سيتحوّل للعمل من جهازه المحلي الآن.
  آخر ما رأيناه من لقطة شاشة على `railway.com/dashboard`: **مشروع
  `my-valentine` (اللي فيه Postgres والمتغيرات الـ12 اللي جهّزناها)
  غير ظاهر إطلاقاً في قائمة المشاريع** — فقط مشروعان فاضيان بلا خدمات
  (`adequate-motivation` و`tender-recreation`)، مع رسالة خطأ حمراء
  **"Free plan resource provision limit exceeded"**. لم نتأكد بعد: هل
  `my-valentine` انحذف فعلياً (بسبب تجاوز حد الموارد؟)، أو إنه موجود
  ومخفي بفلتر ما، أو إن الحساب نفسه تغيّر. **أول شي على الجهاز المحلي**:
  افتح railway.com/dashboard وتأكد من وجود my-valentine. لو موجود:
  أعد محاولة Redeploy بثقة (الكود الحالي مُصلَح فعلياً ومُتحقَّق منه ضد
  PostgreSQL حقيقي محلياً — راجع البند الحرج أعلاه عن `postgresql.ENUM`
  مقابل `sa.Enum`). لو مختفٍ: أنشئه من جديد (Postgres + نفس الـ12
  متغيراً، القائمة الكاملة موجودة في `.env.example`) — الخطة المجانية
  في Railway (Trial، "30 days or $5.00 left" كانت ظاهرة باللقطات)
  ممكن تكون انتهت أو استُهلكت، تأكد من ذلك أولاً بصفحة Billing/Usage
  قبل أي إعداد جديد. المستخدم طلب حذف المشروعين الفاضيين لتحرير حصة
  الموارد — أعطيته خطوات الحذف اليدوي (Project Settings → Danger Zone
  → Delete Project) لكن لم نتأكد بعد إنه نفّذها أو إن my-valentine
  ظهر بعدها.
- **بيئة تطوير محلية داخل الجلسة السحابية نفسها (للمراجعة فقط، غير
  دائمة)**: أثناء طلب المستخدم "فتح المشروع محلياً ليراجعه"، جهّزت
  PostgreSQL 16 (مثبَّتة مسبقاً بالحاوية) + قاعدة بيانات جديدة +
  `.env` محلي، شغّلت الـ backend (uvicorn، منفذ 8000) والـ frontend
  (Vite، منفذ 5173) داخل هذه الجلسة السحابية، وأخذت لقطات Playwright
  حقيقية (تسجيل دخول، لوحة تحكم، شاشة الخدمات بكل الـ39 خدمة) وبعثتها
  للمستخدم. **مهم**: هذه البيئة داخل حاوية الجلسة السحابية المؤقتة —
  غير قابلة للوصول من هاتف/جهاز المستخدم مباشرة، وستختفي بانتهاء الجلسة
  (الحاوية ephemeral). لا تعتمد عليها أي جلسة مستقبلية — لو احتجتوا
  تشغيل محلي فعلي للمراجعة، أعيدوا نفس الخطوات (`service postgresql
  start`، إنشاء user/db، `.env`، `alembic upgrade head`، seed scripts،
  `uvicorn`، `npm run dev`) من الصفر.
- **🟢 النشر التجريبي على Railway نجح فعلياً الآن (`https://paradise1.up.railway.app`)**:
  المستخدم حذف كل مشاريع Railway القديمة وأنشأ مشروعاً جديداً من الصفر
  باتّباع خطوات كاملة أعطيتها له (ربط GitHub بالفرع الصحيح، إضافة
  PostgreSQL، لصق كل متغيرات البيئة دفعة واحدة عبر Raw Editor — بما
  فيها `JWT_SECRET_KEY`/`SIGNED_URL_SECRET` بقيم عشوائية طويلة وّلدتها
  له مباشرة). نجحت الترحيلات الثمانية كاملة **بلا أي خطأ** (تأكيد نهائي
  حي إن إصلاح `postgresql.ENUM` كان صحيحاً)، والخادم بدأ فعلياً
  (`Application startup complete`، منفذ `$PORT` الديناميكي = 8080 كما
  ضبطه Railway، `entrypoint.sh` تعامل معه صح). فعّل رابطاً عاماً عبر
  Networking → Generate Domain، وتأكد المسار الجذر `/` يرجّع
  `{"status":"ok","app":"Wakalat Paradise Platform"}` بنجاح (200). **لم
  نتحقق بعد من `/docs`** (طلبت من المستخدم لقطة منه ولسا ما وصلت).
  بعدها حدّثت `entrypoint.sh` ليشغّل أيضاً `scripts/seed_initial_data`
  و`scripts/seed_agency_services` تلقائياً بعد الترحيلات (كلاهما
  idempotent — يتخطّيان أي بيانات موجودة، يطابق نمط `docker-compose.yml`
  المحلي بالضبط) — بهذا قاعدة بيانات Railway الحقيقية ستُهيَّأ تلقائياً
  (عملات + حساب مدير افتراضي + كتالوج الـ39 خدمة) بأول تشغيل بلا حاجة
  لأي Console/Shell يدوي. تحققت محلياً: شغّلت `sh entrypoint.sh` كاملاً
  ضد قاعدة PostgreSQL المحلية (بعد إعادة تشغيلها) فنجح من الترحيلات
  للتهيئة لبدء الخادم بلا أي خطأ، وأعدت التشغيل مرة ثانية للتأكد من
  الـ idempotency (تجاوَزت البيانات الموجودة كما هو متوقَّع). 83/83
  اختبار محلي ناجح. **هذا التعديل الأخير لم يُدفَع لـ Railway بعد** —
  التالي مباشرة: `git push` هذا التعديل، ثم إعادة نشر Railway مرة أخرى
  (سيُطبَّق تلقائياً عبر GitHub webhook أو Redeploy يدوي)، والتحقق إن
  حساب المدير الافتراضي (`admin@wakalat-paradise.com` / `ChangeMe@2026`
  — **يجب تغييرها فوراً**) وكتالوج الخدمات ظهرا فعلياً على قاعدة بيانات
  Railway الحقيقية. بعدها: نشر الفرونت‌إند (Vercel)، وربط `CORS_ORIGINS`
  و`VITE_API_BASE_URL` بالرابطين الحقيقيين بدل `["*"]`/`localhost`.
- **آخر commit مدفوع**: انظر أعلى "السجل الزمني" أدناه.

## 🔴 مطلوب من الجهاز المحلي الآن (لا يمكن إنجازها من الجلسة السحابية)

جلسة سحابية أنجزت دفعة كبيرة من العمل (تفاصيلها الكاملة في "آخر حالة
معروفة" أعلاه والسجل الزمني أسفل)، لكن توجد أشياء لا تقدر تُنجَز إلا
من جهازك المحلي مباشرة. **اقرأ هذا القسم أولاً قبل أي شي آخر**:

### 1. مزامنة الفرع (إلزامي أولاً)
اسم الفرع تغيّر عدة مرات هذي الجلسة واستقر أخيراً على:
```bash
git fetch origin
git checkout -B claude/wakalat-paradise-project-7iwyup origin/claude/wakalat-paradise-project-7iwyup
```
لا تشتغل على أي اسم فرع قديم (`wakalat-bradis-project`،
`wakalat-baradais-project`، `wakalat-baradise-project` — كلها متروكة وغير مستخدَمة).

### 2. تحديث البيئة المحلية
```bash
pip install -r requirements.txt   # مكتبات جديدة: google-auth, requests
alembic upgrade head              # 3 ترحيلات جديدة: 0006 (متطلبات المستندات)، 0007 (دخول قوقل)، 0008 (حجوزات الطيران)
```

### 3. بيانات اعتماد حقيقية أنت تملكها ولازم تضيفها بنفسك
هذي أشياء بنيت التكامل الكامل لها بالكود، لكن **لا أقدر أو ما ينبغي
أن أخزّن أسرارك الحقيقية في الكود المدفوع لـ GitHub** — لازم تضيفها
في `.env` المحلي عندك (أو في إعدادات النشر لاحقاً):

- **`DUFFEL_API_KEY`**: أعطيتني مفتاح تجريبي (`duffel_test_...`) في
  المحادثة سابقاً لتصحيح تكامل بحث الطيران — لم أحفظه في أي ملف، فقط
  استخدمته للتحقق من آلية الاتصال. ضِفه أنت في `.env` المحلي عندك
  (`DUFFEL_API_KEY=duffel_test_...`)، ثم **جرّب بحث رحلة حقيقي واحد**
  (مثلاً من الدمام DMM إلى إسطنبول IST) من شاشة "طلب جديد" ← اختر خدمة
  "تذاكر طيران". **مهم**: بنيت تحليل استجابة Duffel (`app/services/flight_search_service.py`)
  اعتماداً على توثيقهم المعروف، لأن بيئة الترميز السحابية محجوبة عن
  الإنترنت العام ولم أقدر أختبر استجابة حقيقية مباشرة. لو ظهر أي خطأ
  غريب أو حقول ناقصة، ابعت لي رسالة الخطأ كاملة وأصلحها فوراً.
- **`GOOGLE_CLIENT_ID`** (backend) و**`VITE_GOOGLE_CLIENT_ID`** (frontend،
  في `frontend/.env.local`): لتفعيل "الدخول عبر قوقل". أنشئها من
  Google Cloud Console (مجاني، خطوات كاملة موجودة أعلى هذه المحادثة).
- **`SMTP_HOST`/`SMTP_PORT`/`SMTP_USERNAME`/`SMTP_PASSWORD`/`SMTP_FROM_EMAIL`**:
  لتفعيل إيميلات تأكيد الطلب وتحديث الحالة (الآن HTML بالهوية البصرية
  الكاملة + الشعار). أي حساب Gmail/SendGrid SMTP يفي بالغرض.

### 4. أشياء تحتاج قرارك أنت (مو تقنية)
- **تحديد الأسعار الفعلية** للـ 39 خدمة (لا تزال 0.00$ مؤقتاً).
- **النشر الفعلي (Deployment)** — لا يزال المشروع يشتغل محلياً فقط، لا
  يوجد رابط تقدر ترسله للمدير. أول محاولة كانت عبر Render Blueprint
  (`render.yaml`)، لكن فشلت لأن حساب Render عند المستخدم فيه بالفعل
  قاعدة بيانات مجانية أخرى (الخطة المجانية تسمح بواحدة فقط لكل حساب) —
  حُذف `render.yaml` من المستودع بعدها. الخطة الحالية: **Railway**
  (Backend + PostgreSQL، يقرأ الـ `Dockerfile` الموجود مباشرة بلا أي
  ملف إضافي) + **Vercel** (Frontend الثابت، Root Directory = `frontend`).
  الخطوات الكاملة أعطيت للمستخدم في المحادثة، بانتظار تنفيذها.
- **تغيير كلمة مرور حساب المدير الافتراضي** (`ChangeMe@2026`) قبل أي
  عرض فعلي — موثّقة علناً في README.

## ملاحظات إعداد بيئة محلية (مهمة لأي جهاز جديد)

- استخدم **Python 3.13** لبيئة الـ backend الافتراضية (venv)، **ليس**
  Python 3.14 — بعض المكتبات المثبَّتة (`pydantic-core`, `psycopg2-binary`)
  ما عندها wheels جاهزة لـ 3.14 بعد، وبيحاول pip يبنيها من المصدر ويفشل
  بدون Visual Studio Build Tools.
- `psycopg2-binary` في `requirements.txt` مثبَّت على `2.9.10` (بدل
  `2.9.9` الأصلي) لأن هذا الإصدار فيه wheel جاهز لـ Windows/cp313.
- إذا كان عندك أكثر من نسخة PostgreSQL على الجهاز، تأكد من الـ port
  الصحيح في `postgresql.conf` قبل ضبط `DATABASE_URL` في `.env`.
- سكربتات Python اللي فيها `print()` بالعربي (زي
  `scripts/seed_initial_data.py`) تحتاج `PYTHONIOENCODING=utf-8` قبل
  التشغيل على ويندوز، وإلا بترمي `UnicodeEncodeError` بسبب ترميز cp1252
  الافتراضي.
- **تحذير مهم لأي جهاز ويندوز**: نظام الملفات على ويندوز لا يفرّق بين
  الأحرف الكبيرة والصغيرة. تجنّب إنشاء ملفين بنفس الاسم يختلفان بحرف
  واحد فقط (Case) في نفس المجلد — هذا سبب باگ حقيقي كسر التطبيق بالكامل
  (`AuthContext.tsx` تعارض مع `authContext.ts`، تم حله بإعادة التسمية
  إلى `authContextValue.ts`).
- **`uvicorn --reload` (WatchFiles) ممكن يفوّت تعديلات على ويندوز**: لو
  عدّلت عدة ملفات Python متتالية وظهرت أخطاء 404/سلوك قديم غير متوقع من
  الـ API رغم إن الكود صحيح، جرّب تعيد تشغيل uvicorn يدوياً (kill
  العمليتين reloader+worker وشغّله من جديد) بدل ما تفترض إن فيه باگ في
  الكود.

## المهام المقترحة للجلسة القادمة (على الجهاز المحلي أو أي جلسة تانية)

عشر شاشات مبنية بالكامل (دورة حياة الطلب الكاملة، بحث حجوزات طيران
حقيقي، الهوية البصرية الرسمية...) — انظر "🔴 مطلوب من الجهاز المحلي
الآن" أعلاه للأولويات الفورية. المهام أدناه إضافية، أقل إلحاحاً:

1. **إضافة فحص ثغرات تلقائي للاعتماديات في CI**: `.github/workflows/ci.yml`
   ما فيه `pip-audit` (Python) ولا `npm audit` (frontend). أضف خطوة في
   الـ CI تفشل الـ build لو طلعت ثغرة عالية الخطورة.

2. **مراجعة تخزين المرفقات الحساسة قبل أي نشر إنتاجي حقيقي**:
   `LOCAL_STORAGE_PATH` حالياً تخزين محلي بدون تشفير قرص — مقبول لبيئة
   تطوير بمثيل واحد فقط. قبل أي نشر إنتاجي فعلي (مو Render تجريبي)،
   قيّم إما تشفير القرص أو الانتقال لـ S3 (الطبقة في `app/core/storage.py`
   مصمَّمة أصلاً لتسهيل هذا الانتقال بدون تغيير كود الخدمات المستخدِمة).

3. **مراجعة تجربة الاستخدام للشاشات العشر** حسب ملاحظات استخدام فعلية
   من فريق الوكالة (أو من اختبارك الشخصي)، خصوصاً شاشة الطلبات (بحث
   الطيران + إنشاء الطلب + رفع الدفع) وشاشتي المدفوعات والمستردات.

4. **[تنظيف بيانات اختبار]** أثناء الاختبار الحي لدورة حياة الطلب،
   سجّلت حساب عميل تجريبي `walkthrough-customer@paradise.example`
   (هاتف `0955555501`) وطلبات/مدفوعات تجريبية عليه في قاعدة البيانات
   المحلية على هذا الجهاز فقط (ليس على أي بيئة مشتركة). كمان أثناء
   اختبار إبطال الجلسات سابقاً أنشأت `harden-test@paradise.example`
   (هاتف `0966666601`). احذفهما لو حبيت تنظيف بيانات الاختبار، غير
   ضروري تقنياً. أضفت أثناء اختبار شاشة المستردات كمان
   `refund-customer@paradise.example` (هاتف `0955555502`) بنفس المستوى
   من عدم الأهمية.

---

## السجل الزمني (أضف سطراً جديداً في الأعلى، لا تحذف القديم)

- **2026-07-24 — جلسة سحابية (🟢 نجاح النشر الفعلي على Railway +
  تلقيم البيانات تلقائياً عبر entrypoint.sh)**: المستخدم حذف كل مشاريع
  Railway القديمة وبدأ من الصفر، ونفّذ خطوات كاملة أعطيتها له (ربط
  الفرع الصحيح فوراً بدل الانتظار على `main` الفارغ، إضافة PostgreSQL،
  لصق كل المتغيرات دفعة واحدة عبر Raw Editor مع أسرار عشوائية وّلدتها
  له). **النتيجة: نجحت الترحيلات الثمانية بلا أي خطأ لأول مرة على
  Railway حقيقي**، وبدأ الخادم فعلياً وصار متاحاً على
  `https://paradise1.up.railway.app` (تأكد المسار `/` برجوع
  `{"status":"ok",...}` بنجاح). هذا تأكيد ميداني نهائي إن إصلاح
  `postgresql.ENUM` من الجلسة السابقة كان صحيحاً فعلاً. بعدها لاحظت
  إن قاعدة بيانات Railway الجديدة فاضية تماماً (بلا عملات، بلا حساب
  مدير، بلا كتالوج خدمات) ولا توجد وسيلة Console/Shell واضحة بواجهة
  Railway لتشغيل سكربتات التهيئة يدوياً — فحدّثت `entrypoint.sh`
  ليشغّل `scripts/seed_initial_data` و`scripts/seed_agency_services`
  تلقائياً بعد الترحيلات مباشرة (كلاهما idempotent، نفس نمط
  `docker-compose.yml` المحلي). تحققت محلياً بتشغيل `entrypoint.sh`
  كاملاً مرتين متتاليتين ضد PostgreSQL محلي حقيقي — نجح في المرة الأولى
  (إنشاء البيانات) والثانية (تخطّي كل شي بصمت كما متوقَّع). 83/83
  اختبار محلي ناجح. **لم يُدفَع بعد** — التالي فوراً: `git push`، ثم
  إعادة نشر Railway، والتحقق النهائي إن حساب المدير الافتراضي وكتالوج
  الـ39 خدمة ظهرا على قاعدة البيانات الحقيقية، وتذكير المستخدم بتغيير
  كلمة مرور المدير الافتراضية فور أول دخول.
- **2026-07-24 — جلسة سحابية (🟢 السبب الجذري الحقيقي لباگ 0008 —
  `sa.Enum` بدل `postgresql.ENUM`، + إعداد بيئة محلية للمراجعة بعد يأس
  المستخدم من الاستضافة)**: بعد سلسلة محاولات نشر فاشلة (Railway
  مرتين، ثم `adaptable.io` تبيّن إنها مقفولة، ثم صعوبات ملاحة على
  Zeabur من الهاتف)، أعلن المستخدم إنه يئس مؤقتاً من الاستضافة وطلب
  فتح المشروع محلياً ليراجعه بنفسه. أثناء إعداد بيئة محلية حقيقية
  (PostgreSQL 16 مُثبَّتة مسبقاً بالحاوية، أنشأت مستخدم/قاعدة بيانات
  جديدة تماماً) لتشغيل الـ backend للمراجعة، شغّلت `alembic upgrade
  head` — **وأعدت إنتاج نفس خطأ `DuplicateObject` بالضبط، على قاعدة
  فاضية 100% لم تُلمَس من قبل إطلاقاً**. هذا نسف نظرية "Railway يبني
  كود قديم" بالكامل: الباگ حتمي من أول تشغيل، بغض النظر عن أي بقايا.
  السبب الحقيقي: ملف `0008_add_flight_booking_tables.py` استخدم
  `sa.Enum(..., create_type=False)` — لكن `create_type` **معامل خاص
  بـ `postgresql.ENUM` تحديداً**؛ تمريره لـ `sa.Enum` العام (متعدد
  قواعد البيانات) يُقبَل بصمت بلا أي تأثير فعلي، فـ `op.create_table()`
  استمر يصدر `CREATE TYPE` تلقائياً رغم وجود الكود. هذا بالضبط الفرق
  بين 0008 (فشلت) و`0001_initial_schema.py` (نجحت من أول يوم — تستخدم
  `postgresql.ENUM(..., create_type=False)` الصحيح لكل أنواعها). صُحح
  الاستيراد والنوع في 0008 ليطابق نمط 0001 تماماً (مع الإبقاء على DO
  block من محاولة سابقة كطبقة حماية إضافية). تحققت فعلياً: مسحت قاعدة
  PostgreSQL المحلية بالكامل، أعدت إنشاءها فاضية، وشغّلت `alembic
  upgrade head` من الصفر — **نجحت من أول محاولة بلا أي خطأ** (فحصت
  `alembic current` = `0008_add_flight_booking_tables (head)`)، وأعدت
  التشغيل مرة ثانية (idempotent) بلا مشاكل. 83/83 اختبار محلي لا زال
  ناجح. دفعت الإصلاح (راجع تفاصيله الكاملة في "آخر حالة معروفة" أعلاه).
  **هذا هو الإصلاح النهائي المؤكَّد ضد قاعدة بيانات حقيقية فعلياً** —
  أي نشر مستقبلي على أي منصة سينجح من أول مرة. التالي: إكمال تجهيز
  البيئة المحلية (seed، تشغيل frontend، اختبار حي عبر Playwright) لعرض
  التطبيق على المستخدم للمراجعة، ثم لاحقاً إعادة محاولة النشر الفعلي
  (Zeabur أو Railway) بثقة كاملة إن الترحيل سينجح.
- **2026-07-24 — جلسة سحابية (هجرة خطة النشر: Railway → adaptable.io
  (مقفولة) → Zeabur، + entrypoint.sh لتشغيل الترحيلات تلقائياً)**:
  بعد محاولتين فاشلتين على Railway بنفس خطأ `DuplicateObject` (راجع
  السطرين التاليين)، ولاحظنا صعوبة التأكد من واجهة Railway المعقدة إذا
  كانت تبني آخر commit مدفوع أصلاً، طلب المستخدم تجربة منصة بديلة —
  فزهج من Railway. جرّبنا `adaptable.io` أولاً بناءً على اقتراحه، لكن
  `WebFetch` رجّع `ENOTFOUND` على النطاق (وعلى `www.` أيضاً) — أي إنها
  **مقفولة فعلياً وليست مجرد بطيئة أو معطّلة مؤقتاً**. اقترح المستخدم
  بعدها `zeabur.com` — تحقّقت إن نطاقها حي (يرد 403 بسبب حماية بوتات لا
  DNS)، وأكّدت إنها منصة نشر حقيقية تدعم Dockerfile + قواعد بيانات
  مُدارة. وافق المستخدم على البدء بها. **قبل أي إعداد فعلي على Zeabur**،
  لاحظت فجوة مهمة كانت ستسبب فشل نشر صامت على أي منصة جديدة: الترحيل
  التلقائي (`alembic upgrade head`) على Railway كان يعتمد بالكامل على
  "Start Command" مخصّص أُعِدّ يدوياً من واجهة Railway نفسها (لا وجود له
  بأي ملف بالمستودع، فما ينتقل تلقائياً لأي منصة جديدة). أضفت
  `entrypoint.sh` بجذر المشروع (يشغّل `alembic upgrade head` ثم
  `uvicorn` بمنفذ `$PORT` من البيئة أو 8000 افتراضياً) وحدّثت
  `Dockerfile` (`CMD ["./entrypoint.sh"]` بدل تشغيل `uvicorn` مباشرة) —
  الآن الترحيل جزء أصيل من صورة الـ Docker، يعمل على أي منصة Dockerfile
  بلا أي إعداد يدوي إضافي منسي. `docker-compose.yml` المحلي غير متأثر.
  تحققت من 83/83 اختبار محلي ناجح. التالي: تسجيل حساب Zeabur وربط
  الـ repo والفرع، إضافة PostgreSQL، ضبط متغيرات البيئة (القائمة في
  `.env.example`)، والنشر — بانتظار لقطات شاشة من المستخدم للمتابعة
  خطوة بخطوة.
- **2026-07-24 — جلسة سحابية (المحاولة الثانية للإصلاح: نفس خطأ
  DuplicateObject تكرر بعد إصلاح create_type=False)**: المستخدم أعاد
  محاولة نشر Railway بعد الإصلاح السابق (`create_type=False`)، لكن
  ظهر **نفس الخطأ حرفياً**: `psycopg2.errors.DuplicateObject: type
  "flight_booking_fee_type" already exists` على نفس عبارة
  `CREATE TYPE ... AS ENUM ('flat', 'percentage')` — يعني `checkfirst=True`
  في استدعاء `.create()` اليدوي نفسه ما كان يتجاوز النوع الموجود فعلياً
  رغم كل الترحيلات 0001-0007 تنجح من الصفر في كل محاولة (log يوضّح كل
  خطوة تُعاد من البداية). بدل تتبّع سبب فشل `checkfirst` الدقيق (قد
  يرجع لبقاء النوع من محاولة نشر أقدم قبل هذي التعديلات)، استبدلت
  الإنشاء بالكامل بأمر SQL خام مضمون النتيجة بغض النظر عن سلوك
  SQLAlchemy: `DO $$ BEGIN CREATE TYPE ... EXCEPTION WHEN duplicate_object
  THEN null; END $$;` (ونفس المبدأ بالعكس لـ `DROP TYPE IF EXISTS` في
  `downgrade()`). هذا يضمن نجاح الترحيل بغض النظر إذا كان النوع موجوداً
  مسبقاً أو لا. تحققت أن 83/83 اختبار محلي لا زالت ناجحة (SQLite لا
  تستخدم Alembic). التالي: دفع هذا الإصلاح وإعادة محاولة نشر Railway؛
  لو فشل بخطأ مختلف بعد كذا، تأكدوا أولاً من رقم الـ commit اللي Railway
  يبني منه فعلياً بتبويب Deployments قبل افتراض باگ جديد.
- **2026-07-24 — جلسة سحابية (إصلاح باگ ثانٍ في النشر: تكرار CREATE TYPE
  في ترحيل Alembic)**: بعد إصلاح باگ طول معرّف الترحيل `0003` (مذكور
  بالسطر التالي)، أعاد المستخدم محاولة نشر Railway ووصل لخطوة لاحقة، لكن
  `alembic upgrade head` فشل هذه المرة برسالة
  `psycopg2.errors.DuplicateObject: type "flight_booking_fee_type" already exists`
  أثناء تنفيذ ترحيل `0008_add_flight_booking_tables`. السبب: عرّف
  `flight_booking_fee_type = sa.Enum(...)` بلا `create_type=False`، فـ
  `.create(checkfirst=True)` اليدوي بأول سطر من `upgrade()` أنشأ النوع
  بنجاح، ثم `op.create_table()` بعده حاول ينشئه **مرة ثانية** تلقائياً
  (لأن أي `Enum` بدون `create_type=False` يصدر DDL خاص به أيضاً عند
  إنشاء أي جدول يستخدمه) — نفس النمط الصحيح المستخدَم بكل أنواع
  `0001_initial_schema.py` (`create_type=False` في كل واحد منها)، فقط
  0008 نسيتها. أصلحته بإضافة `create_type=False`. تفصيلة مهمة: المحاولة
  الفاشلة سبق ونجحت فعلياً بإنشاء النوع في قاعدة Railway الحقيقية قبل ما
  تفشل على الجدول، لكن **لا حاجة لأي تصحيح SQL يدوي** هذه المرة —
  `checkfirst=True` الموجود أصلاً بالكود سيتجاوز النوع الموجود بصمت،
  و`create_type=False` يمنع محاولة الإنشاء المكرر عند إنشاء الجدول.
  التالي: دفع هذا الإصلاح وإعادة محاولة نشر Railway.
- **2026-07-24 — جلسة سحابية (محاولة النشر التجريبي: Render → Railway، وإصلاح
  باگ حقيقي في معرّف ترحيل Alembic)**: المستخدم طلب نشر نسخة تجريبية حقيقية
  للعرض على المدير. المحاولة الأولى عبر Render Blueprint (`render.yaml`)
  فشلت لأن حساب Render عند المستخدم فيه مسبقاً قاعدة بيانات مجانية أخرى
  (الخطة المجانية تسمح بواحدة فقط لكل حساب) — حُذف `render.yaml` والخطة
  تحوّلت لـ **Railway** (Backend + PostgreSQL، يقرأ `Dockerfile` الموجود
  مباشرة بلا أي ملف إضافي) + **Vercel** (Frontend لاحقاً). أثناء إعداد
  Railway خطوة بخطوة عبر لقطات شاشة من هاتف المستخدم (لا يوجد وصول مباشر
  لي لأي من المنصتين — لا Render ولا Railway لهما تكامل API متصل بهذي
  الجلسة، وحتى تجربتها مباشرة بمفتاح API افتراضي رجعت 403 من سياسة شبكة
  الجلسة نفسها)، واجهنا 3 مشاكل بالتتابع: (1) Render/Railway كانا يحاولان
  النشر من فرع `main` الفارغ (فيه بس README) بدل فرع العمل الفعلي — كلاهما
  فشل لنفس السبب بالضبط. (2) بعد تصحيح الفرع، فشل تشغيل Railway لأن متغيرات
  البيئة الأساسية ما كانت مضبوطة بعد. (3) بعد ضبطها، فشل التطبيق فعلياً
  بمحاولة اتصال بقاعدة بيانات على `localhost:5432` — تبيّن أن `DATABASE_URL`
  انكتب كنص حرفي (نفس قيمة `.env.example` التوضيحية) بدل ربطه كمرجع فعلي
  بخدمة Postgres (`${{Postgres.DATABASE_URL}}`)، صُحح عبر ميزة "Add Variable"
  في Railway. **بعد كل هذا، ظهر باگ حقيقي بالكود نفسه** (وليس بإعدادات
  النشر): فشل `alembic upgrade head` برسالة
  `psycopg2.errors.StringDataRightTruncation` — معرّف الترحيل
  `0003_expand_service_category_enum` طوله 33 حرف، أطول من الحد الافتراضي
  لعمود `alembic_version.version_num` في Alembic (`VARCHAR(32)`). SQLite
  (المستخدَم في كل اختبارات pytest المحلية) لا يفرض حدود طول الأعمدة
  إطلاقاً، فما ظهر هذا الباگ من قبل رغم مرور الترحيل بـ 83 اختبار محلي —
  أول مرة اكتُشف كان أثناء أول تشغيل فعلي لـ `alembic upgrade head` على
  PostgreSQL حقيقي. صُحح بتقصير المعرّف إلى `0003_expand_service_category`
  (29 حرف، الملف أُعيدت تسميته أيضاً) وتحديث `down_revision` في
  `0004_add_service_discount_fields.py` ليطابق. **تفاصيل مهمة جداً لأي
  بيئة (جهاز محلي أو خادم) طبّقت `alembic upgrade head` على PostgreSQL
  حقيقي قبل هذا الإصلاح ووصلت فعلياً لهذا الترحيل**: راجع الملاحظة الحرجة
  المفصّلة أعلى في "آخر حالة معروفة" (فيها أمر SQL التصحيح اليدوي
  المطلوب). تحققت أن سلسلة الترحيلات كاملة تحل بشكل صحيح بعد التعديل
  (`alembic history` على SQLite مؤقت) وأن كل الـ 83 اختبار لا زالت ناجحة
  (SQLite لا تستخدم Alembic أصلاً في الاختبارات، فما تأثرت). التالي: إعادة
  محاولة نشر Railway بعد هذا الإصلاح (بانتظار رد فعل النشر التلقائي بعد
  الدفع لهذا الفرع)، ثم Vercel للفرونت‌إند، ثم ربط الرابطين وتحديث
  `CORS_ORIGINS`.
- **2026-07-23 — جلسة سحابية (بحث حجوزات طيران حقيقي + رسم حجز + رقم واتساب)**:
  بعد سلسلة أسئلة من المستخدم عن جاهزية المشروع للعرض على المدير (راجع
  محادثة الجلسة)، اتضح أن أهم فجوة هي: خدمة "تذاكر طيران" سطر عام واحد
  بلا بحث/أسعار حقيقية إطلاقاً. طلب المستخدم حلاً بنفس فكرة مواقع
  الحجز (كل الخطوط والرحلات المتاحة بأسعارها)، مع تحكّم المدير برسم حجز
  (فلات أو نسبة، حسب كل حالة)، ورقم واتساب إلزامي من العميل. أكّد عدم
  وجود اشتراك GDS حالياً، فاخترنا Amadeus for Developers (تسجيل مجاني
  فوري، Test environment يعمل الآن)، مع إبقاء الحجز الفعلي (إصدار
  التذكرة) يدوياً كما هو معمول به حالياً — البحث فقط جديد، لا حجز/دفع
  آلي. بنيت: عميل Amadeus (`app/integrations/amadeus_client.py`، توكن
  OAuth2 مخزَّن بالذاكرة)، إعداد رسم حجز وحيد قابل للتعديل من المدير في
  أي وقت (`flight_booking_fee_settings`، فلات/نسبة)، جدول تفاصيل الرحلة
  لكل طلب (`flight_booking_details`)، وعمود `orders.contact_whatsapp`
  إلزامي على مستوى الـ API لكل الطلبات (ترحيل
  `0008_add_flight_booking_tables`). `order_service.create_order`
  يرفض طلبات فئة تذاكر الطيران/البواخر بلا بيانات رحلة مرفَقة (والعكس)،
  ويحسب السعر من `السعر الحقيقي + الرسم` بدل سعر الخدمة الثابت. Frontend:
  قسم بحث رحلات مدموج تلقائياً في "طلب جديد" عند اختيار خدمة تذاكر،
  بطاقة عرض/تغيير الرحلة المختارة، حقل واتساب إلزامي لكل الطلبات، وشاشة
  "رسوم الحجز" الجديدة (admin فقط). غيّرت فيكستشر `sample_service`
  المشترك في الاختبارات من تصنيف `flight` إلى `insurance` لأن أغلب
  اختبارات الطلبات القديمة عامة وليست عن حجوزات الطيران تحديداً. أضفت
  3 ملفات اختبار جديدة (Amadeus مُحاكى بالكامل عبر monkeypatch، لا حاجة
  لمفاتيح حقيقية) — **81/81 اختبار ناجح**. اختبرت حياً: البحث بلا
  مفاتيح Amadeus يرجع 503 برسالة عربية واضحة (تأكدت منها في الـ API
  وفي الواجهة)، تحديث رسوم الحجز (فلات ثم نسبة) يعمل ويُحفظ، وإنشاء طلب
  فعلي عبر API ببيانات رحلة (محاكية لما سيرسله بحث حقيقي لاحقاً بمجرد
  توفر مفاتيح Amadeus) أعطى `total_amount` صحيحاً (340 + 15 = 355).
  `pytest`/`npm run build`/`oxlint`/`tsc --noEmit` كلها نظيفة. **مهم
  لأي جهاز يسحب هذا الكود**: ترحيل جديد `0008_add_flight_booking_tables`
  — لازم `alembic upgrade head`. التالي: توفير بيانات اعتماد Amadeus
  فعلية (حساب مجاني على developers.amadeus.com) لتفعيل البحث الحقيقي،
  أو أي أولوية أخرى من قائمة "المهام المقترحة" أعلاه.
- **2026-07-23 — جلسة سحابية (أولوية الهاتف + قوقل + إيميل + متطلبات
  المستندات)**: المستخدم لاحظ أني كنت أراجع/أطوّر بمنظور شاشة حاسوب رغم
  إن الهاتف هو الأكثر استخداماً، وطلب أربعة أشياء معاً: (1) مراجعة
  الواجهة بأولوية الهاتف، (2) تسجيل دخول بسيط عبر قوقل للزوار الذين
  يحجزون تذكرة/فيزا، (3) إشعارات إيميل لتأكيد الطلب وتحديثات الحالة،
  (4) إضافة نص متطلبات المستندات الكامل الذي أرسله (لكل تصنيفات
  التذاكر/الفيزات/الإقامات/التجديد/الموافقات الأمنية/البكجات/الاستخراج/التوثيق)
  كمحتوى قابل للتعديل الدائم من الإدارة. بما إن قوقل والإيميل يحتاجان
  بيانات اعتماد فعلية (Client ID، حساب SMTP) وما كانت متوفرة بعد، سألت
  المستخدم فوافق على المضي بالخيارين الموصى بهما: env vars فارغة
  للتكاملين (تعمل بمجرد توفر بيانات حقيقية لاحقاً، بلا توقف الآن).
  نفّذت الأربعة بالكامل — التفاصيل التقنية الكاملة موثّقة في "آخر حالة
  معروفة" أعلاه (قسم "جديد"). لخّص الأهم: (أ) اكتشفت وأصلحت أثناء بناء
  قوقل ثغرة حقيقية كانت ستظهر لاحقاً — `auth_service.authenticate` كان
  سيرمي 500 غير معالَج لمستخدم Google-only حاول تسجيل الدخول التقليدي.
  (ب) طابقت محتوى متطلبات المستندات الذي أرسله المستخدم حرفياً مع
  عناوين الـ 39 خدمة الموجودة أصلاً في `scripts/agency_services_catalog.py`
  (تحقّقت آلياً أن كل خدمة لها بند متطلبات، وكل بند متطلبات يطابق خدمة
  حقيقية — لا نقص ولا زيادة). (ج) اختبرت أولوية الهاتف حياً بمتصفح
  Playwright على viewport هاتف حقيقي (iPhone-size 390×844): تسجيل دخول،
  فتح القائمة المنسدلة، التنقّل بين الشاشات، فتح "طلب جديد" ورؤية قائمة
  المستندات المطلوبة — صفر تمرير أفقي (overflow) في كل شاشة. أضفت 3
  ملفات اختبار جديدة — **69/69 اختبار ناجح**. `pytest`/`npm run build`/
  `oxlint`/`tsc --noEmit` كلها نظيفة. **مهم لأي جهاز يسحب هذا الكود**:
  ترحيلان جديدان (`0006_add_service_requirements`،
  `0007_add_google_login_support`) — لازم `alembic upgrade head`. التالي:
  توفير بيانات اعتماد Google/SMTP فعلية لتفعيل التكاملين، أو أي أولوية
  أخرى من قائمة "المهام المقترحة" أعلاه (أهمها تحديد الأسعار الفعلية).
- **2026-07-23 — جلسة سحابية (شاشة المستردات Refunds)**: المستخدم طلب
  البدء في شاشة المستردات مباشرة بعد إكمال دورة حياة الطلب. الـ backend
  كان جاهزاً بالكامل من قبل (`refund_service.py`) لكن بلا مسار GET
  لعرض القائمة، وبلا أي واجهة إطلاقاً. أضفت `GET /api/v1/refunds`
  (staff، تصفية اختيارية بالحالة)، خاصية `Refund.order_number` محسوبة،
  وأضفت `refunds: list[RefundOut]` لـ `OrderOut` عشان العميل يشوف حالة
  استرداده من نفس شاشة تفاصيل طلبه. Frontend: نموذج طلب استرداد داخل
  لوحة تفاصيل الطلب (يظهر فقط لطلب مؤهَّل بلا استرداد مفتوح بالفعل)،
  وشاشة "المستردات" الجديدة تعكس دورة العمل ذات الخطوتين من الـ backend
  عمداً: اعتماد/رفض أولاً، ثم زر "تنفيذ الاسترداد" منفصل يظهر فقط بعد
  الاعتماد (التنفيذ هو اللي فعلياً ينقل الطلب لـ refunded ويرجّع
  المبلغ لمحفظة الوكيل إن انطبق). زرا الاعتماد/الرفض/التنفيذ admin
  فقط في الواجهة (أعلى صرامة من المدفوعات اللي يكفيها موظف عادي، مطابقةً
  لـ `require_admin` بالـ backend). واجهت مشكلة بسيطة أثناء اختبار
  الـ Playwright نفسه (مو في الكود): بعد اعتماد الطلب يختفي من فلتر
  "قيد المراجعة" لأنه فعلياً يتحرك لفلتر "معتمَدة" — هذا سلوك صحيح
  ومقصود، اضطررت بس أبدّل الفلتر لـ"الكل" في سكربت الاختبار قبل اختيار
  الصف للتنفيذ. اختبرت الدورة كاملة حياً بمتصفح حقيقي من الصفر: عميل
  أنشأ طلباً ورفع دفعاً، مدير اعتمد الدفع، عميل قدّم طلب استرداد بسبب
  "تغيّر موعد السفر"، مدير اعتمد الطلب ثم نفّذه، وتأكدت إن الطلب انتقل
  فعلياً لحالة "مسترجَع" (نهائية) وإن سجل الاسترداد يظهر بحالة "منفَّذ"
  داخل تفاصيل الطلب. أضفت اختبارين في `tests/test_refund_service.py`
  — **52/52 اختبار ناجح**. `npm run build` و`oxlint` نظيفان. التالي:
  تحديد الأسعار الفعلية، أو النشر الفعلي (Deployment)، أو أي أولوية
  من قائمة المهام المقترحة أعلاه.
- **2026-07-23 — جلسة سحابية (دورة حياة الطلب الكاملة: إنشاء → دفع →
  مراجعة → في السيستم → رفع مستند → اكتمال)**: المستخدم وافق على خطة
  بناء شاشتي "طلب جديد" و"الدفع/المراجعة اليدوية"، وأضاف تفصيلاً حاسماً
  غيّر التصميم: **الموظف عادة يحجز فعلياً من موقع خارجي (ناقل/سفارة)،
  يحتاج بيانات الطلب/المسافرين لينفّذ الحجز هناك، وبعد ما يخلص يرجع
  يرفع التذكرة/الفيزا الصادرة في نظامنا** — لاحظت إن هذا يطابق حرفياً
  حالة `in_system` الموجودة أصلاً في `ALLOWED_TRANSITIONS` (لم أكن
  أعرف لماذا صُمِّمت هذه الحالة تحديداً قبل هذا التوضيح). بنيت: (1)
  Backend: عمود `Order.deliverable_file_url` جديد (ترحيل
  `0005_add_order_deliverable_file`) مع `deliverable_signed_url`
  محسوبة، `POST /api/v1/orders/{id}/deliverable` (staff، يرفض إلا إذا
  `in_system`/`completed`)، وأصلحت ثغرة استخدام حقيقية: `PaymentOut`
  ما كان يُرجع أي رابط لصورة إشعار الدفع أصلاً — أضفت `receipt_signed_url`
  و`order_number` محسوبَين، و`GET /api/v1/payments` (staff) كطابور
  مراجعة. (2) Frontend: "طلب جديد" في شاشة الطلبات، نموذج رفع دفع
  multipart داخل لوحة تفاصيل الطلب، شاشة "المدفوعات" الجديدة كاملة
  (قائمة + معاينة إيصال + اعتماد/رفض)، ونموذج رفع المستند النهائي
  (staff فقط، يظهر عند `in_system`) + رابط تحميل يظهر للجميع (شاملاً
  العميل). اكتشفت وأصلحت فخّاً حقيقياً: الروابط الموقّتة القادمة من
  الـ backend نسبية (`/api/v1/files/...`) وتفشل بصمت لو استُخدمت مباشرة
  في `<img>`/`<a>` لأن الفرونت‌إند على منفذ مختلف — أضفت `buildFileUrl()`
  في `apiClient.ts` لدمجها مع عنوان الـ backend الفعلي. اختبرت الدورة
  الكاملة حياً بمتصفح Playwright حقيقي من الصفر للنهاية بحسابين
  مختلفين (عميل ثم مدير): إنشاء طلب ($250 تذاكر طيران) → رفع إثبات
  دفع بنكك → اعتماد الدفع (انتقال تلقائي لـ processing) → نقل لـ
  in_system → رفع تذكرة PDF وهمية → نقل لـ completed → رجعت سجّلت
  دخول كالعميل نفسه وتأكدت إنه يشوف "مكتمل" ورابط تحميل حقيقي يرجّع
  PDF بحالة 200. أضفت 8 اختبارات جديدة (`test_order_deliverable_service.py`،
  `test_payment_service.py`) — **50/50 اختبار ناجح**. `npm run build`
  و`oxlint` نظيفان. التالي: تحديد الأسعار الفعلية، أو شاشة الاسترداد
  (Refunds)، أو النشر الفعلي.
- **2026-07-23 — جلسة سحابية (إكمال إدارة الخدمات: حذف + عروض خصم + شاشة)**:
  المستخدم سأل مباشرة "هل تأكدت إن حساب المدير يقدر يحدّث/يعدّل/يحذف
  الخدمات ويقدّم عروض خاصة؟" — راجعت وطلعت الإجابة: لا، جزئياً بس.
  التعديل كان موجود، لكن **الحذف والعروض الخاصة ما كانا موجودين إطلاقاً
  في الـ backend، ولا حتى شاشة إدارة في الـ Frontend للتعديل الموجود
  أصلاً**. أكملت الأربعة: (1) `DELETE /api/v1/services/{id}` (admin) —
  يتحقق أولاً من عدم وجود طلبات مرتبطة بالخدمة (`orders.service_id`)
  ويرفض بـ 409 مع رسالة تقترح التعطيل بدلاً من الحذف لو وُجدت، حفاظاً
  على سلامة سجل الطلبات التاريخي. (2) عروض خصم محدودة المدة: حقلا
  `discount_percentage`/`discount_valid_until` على `Service` (ترحيل
  `0004_add_service_discount_fields`)، خاصيتا `has_active_discount`/
  `effective_price_usd` محسوبتان مع معالجة صريحة لفارق naive/aware
  datetime بين SQLite (الاختبارات) وPostgreSQL (الإنتاج) لتفادي انهيار
  المقارنة، ومسار `PATCH /api/v1/services/{id}/discount` (admin) يتحقق
  عبر Pydantic `model_validator` إن الحقلين يُرسلان معاً أو يُتركان
  فارغين معاً (لا مزيج بينهما). (3) `GET /api/v1/services/manage/all`
  (staff) يُعيد كل الخدمات شاملة المعطَّلة لأغراض الإدارة. (4) شاشة
  "الخدمات" الكاملة في الـ Frontend (`frontend/src/features/services/`:
  ServicesPage + ServicesTable بفلاتر تصنيف + CreateServiceModal +
  EditServiceModal + DiscountModal + ConfirmDialog جديد قابل لإعادة
  الاستخدام)، مع تقييد زري الخصم/الحذف على admin فقط في الواجهة (مطابقةً
  لقيود require_admin بالـ backend، بدل إظهار أزرار سترجع 403 دائماً
  للموظف). اختبرت الأربعة حياً بمتصفح Playwright حقيقي على backend
  حقيقي: أنشأت خدمة، عدّلت سعرها، فعّلت خصم 25% على "تأشيرة تركيا"،
  حذفت الخدمة التجريبية بنجاح (بلا طلبات)، ثم أنشأت طلباً فعلياً على
  "تأشيرة تركيا" وتأكدت إن محاولة حذفها بعدها تُرفض بـ 409 بالضبط كما
  هو مصمَّم. أضفت `tests/test_service_service.py` (6 اختبارات) —
  **42/42 اختبار ناجح**. `npm run build` و`oxlint` نظيفان. التالي:
  انظر "المهام المقترحة" أعلاه — أهمها تحديد الأسعار الفعلية.
- **2026-07-23 — جلسة سحابية (ترقية PyJWT + كتالوج الخدمات الفعلي)**:
  بناءً على طلب مباشر من المستخدم: (1) رقّيت `python-jose` إلى
  `PyJWT==2.9.0` في `app/core/security.py` و`requirements.txt` (استبدلت
  `from jose import jwt, JWTError` بـ `import jwt` + `except jwt.PyJWTError`)،
  وتحققت حياً إن توليد/فك تشفير التوكن يعمل بعد إزالة `python-jose`
  والاعتماديات المرتبطة به (`ecdsa`, `rsa`, `pyasn1`) من البيئة تماماً.
  (2) أضفت كتالوج خدمات الوكالة الفعلي كاملاً كما أرسلته الوكالة نصاً:
  وسّعت `ServiceCategory` بـ 7 تصنيفات جديدة، أضفت ترحيل
  `0003_expand_service_category_enum` (تحقق منه بـ `alembic upgrade
  head --sql` — 7 عبارات `ALTER TYPE ... ADD VALUE` صحيحة)، وبنيت
  `scripts/agency_services_catalog.py` (بيانات الـ 39 خدمة) +
  `scripts/seed_agency_services.py` (منطق الإدخال، idempotent). اختبرت
  حياً: شغّلت السكربت مرتين على SQLite (تأكدت من عدم التكرار)، شغّلت
  الـ backend فعلياً وطلبت `GET /api/v1/services` (رجعت الـ 39 خدمة
  بالتصنيفات وتفاصيل الفيزا/الإقامة الصحيحة)، وأنشأت طلباً حقيقياً
  بخدمة من تصنيف جديد كلياً (`attestation`) عبر `POST /api/v1/orders`
  ونجح بالكامل. أضفت `tests/test_agency_services_catalog.py` (4
  اختبارات: تفرّد العناوين، صحة التصنيفات، idempotency، وربط/عدم ربط
  تفاصيل الفيزا حسب التصنيف) — **36/36 اختبار ناجح**. حدّثت
  `frontend/src/types/enums.ts` بالتصنيفات الجديدة و`npm run build`
  نظيف. أضفت خطوة `seed_agency_services` لسلسلة أوامر `docker-compose.yml`.
  **الأسعار كلها 0.00 مؤقتاً** — لم تُرسَل أسعار فعلية، لازم الإدارة
  تحدّثها قبل أي عرض حقيقي للعملاء. التالي: انتظار توجيه المستخدم
  (ربما تحديد الأسعار الفعلية، أو بناء شاشة عرض الخدمات العامة في
  الـ Frontend، أو أي أولوية من قائمة المهام المقترحة أعلاه).
- **2026-07-23 — جلسة سحابية (تحصين رفع الملفات + إبطال الجلسات)**:
  بصفتي مستشاراً تقنياً، راجعت الأمان مرة أخرى بعد سؤال مباشر من
  المستخدم ولقيت سلسلة ثغرات حقيقية قابلة للتسلسل: `save_private_file`
  ما كان يتحقق من نوع/حجم الملف المرفوع، و`/api/v1/files` كان يقدّم
  الملفات بـ Content-Type مخمَّن من نظام التشغيل بدون `nosniff` — يعني
  لو حد رفع ملف `.html`/`.svg` باسم "إشعار دفع" وفتحه موظف مراجعة في
  متصفحه، كان ممكن ينفّذ JS مخزَّن (Stored XSS). وبما إن توكن الدخول
  محفوظ بـ `localStorage` (لا httpOnly cookie)، ده كان يعني سرقة توكن
  الموظف/المدير مباشرة — استحواذ كامل على الحساب لمدة تصل 12 ساعة بدون
  أي طريقة لإبطاله. أصلحت الثلاثة معاً: (1) `storage.py` صار يرفض أي
  ملف مرفوع نوعه خارج jpg/png/webp/pdf أو حجمه أكبر من 5MB، (2)
  `files.py` صار يفرض Content-Type من امتداد الملف المخزَّن (مش تخمين
  النظام)، (3) أضفت middleware ترويسات أمان قياسية في `main.py`
  (X-Content-Type-Options: nosniff, X-Frame-Options: DENY,
  Referrer-Policy, Strict-Transport-Security). كمان لاحظت إن تغيير
  كلمة المرور ما كان يُبطل التوكنات القديمة (JWT عديم الحالة) — فلو
  توكن اتسرق فعلاً، تغيير كلمة المرور ما كان يحميك لمدة تصل 12 ساعة.
  أضفت `token_version` لجدول users (ترحيل `0002_add_user_token_version`،
  **لازم `alembic upgrade head` على أي جهاز جديد**)، يُضمَّن في كل JWT
  كـ claim `tv`، ويُقارَن في `get_current_user`؛ تغيير كلمة المرور يزيده
  فيُبطل كل توكن قديم فوراً، والـ endpoint نفسه يُرجع توكناً جديداً
  ليستمر من غيّر كلمة مروره بدون انقطاع (عدّلت الفرونت‌إند: أضفت
  `refreshToken` لـ `AuthContext` يستدعيه `useChangePassword` بدل
  الاعتماد على `login` الكامل). اختبرت **حياً على PostgreSQL الحقيقي**
  (مو SQLite بس): سجّلت دخول عبر واجهة تسجيل الدخول الفعلية، غيّرت كلمة
  المرور من شاشة "حسابي" الحقيقية، تأكدت إن الجلسة استمرت بدون تسجيل
  خروج قسري، وتأكدت مباشرة عبر fetch إن التوكن القديم يُرفَض تحديداً
  برسالة "انتهت صلاحية هذه الجلسة" (مو خطأ فك تشفير عام) بينما التوكن
  الجديد يعمل. أضفت 5 اختبارات جديدة (`test_storage.py` × 3،
  `test_change_password_increments_token_version`،
  `test_password_change_invalidates_old_token_and_issues_new_one`) —
  **31/31 اختبار ناجح**. `npm run build` نجح بدون أخطاء TypeScript.
  التالي: التوصيات الأقل أولوية أعلاه، أو مراجعة تجربة الاستخدام، أو
  أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (قفل صفوف محفظة الوكيل)**: نفّذت آخر توصية
  مؤجَّلة من مراجعة الأمان: `wallet_service.deduct_for_order` كان يقرأ
  `agent.wallet_balance` من الجلسة بدون قفل، فلو وصل طلبان لنفس الوكيل
  في نفس اللحظة (نفس الرصيد أو الحد الائتماني)، كل واحد يمر فحص الكفاية
  منفرداً ويتجاوز الاثنان معاً الحد المسموح. أضفت
  `db.query(AgentProfile).filter(...).populate_existing().with_for_update().one()`
  قبل الفحص/الخصم — `with_for_update()` بيسوي `SELECT ... FOR UPDATE`
  حقيقي على PostgreSQL (يقفل الصف حتى commit/rollback الجلسة)، وعلى
  SQLite (بيئة الاختبارات) بيتجاهله SQLAlchemy بصمت بدون خطأ، فالاختبارات
  الحالية الأربعة في `test_wallet_service.py` ضلت تعمل بدون تعديل.
  `populate_existing()` ضروري مع `with_for_update()` — بدونه SQLAlchemy
  يرجّع نفس الكائن المخزَّن بالـ identity map بقيمه القديمة بدل قراءة
  الصف الطازج بعد القفل. 26/26 اختبار ناجح (شاملة `test_order_service.py`
  اللي بيغطي مسار إنشاء الطلب كاملاً مع الخصم). **ملاحظة**: التزامن
  الحقيقي غير قابل للاختبار على SQLite (الاختبارات كلها على نفس
  الاتصال)، فالتحقق هنا منطقي/بمراجعة الكود، مو باختبار تزامن فعلي —
  لو حبيتوا تأكيد إضافي، يحتاج اختبار يدوي على PostgreSQL بطلبين
  متزامنين فعلاً. التالي: مراجعة تجربة الاستخدام، أو أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (شاشة حسابي)**: أضفت `PATCH
  /api/v1/users/me/password` لتغيير كلمة المرور الذاتي (ما كان موجوداً
  إطلاقاً في الـ backend) مع تحقق من كلمة المرور القديمة ورفض تكرار
  نفس الكلمة، وبنيت شاشة "حسابي" الكاملة: بيانات شخصية، كرت ملف
  الوكالة B2B (لحسابات agent فقط، من `GET /agents/me`)، وفورم تغيير
  كلمة المرور. اختبرت الاثنين (عميل عادي ووكيل) يدوياً في المتصفح
  بحسابات حقيقية — كل شيء شغال. حذفت `ComingSoonPage.tsx` (صار كود
  ميت بعد اكتمال آخر شاشة). 26/26 اختبار ناجح. **ملاحظة تشغيلية
  مهمة**: واجهت حالة فيها `uvicorn --reload` ما التقط تعديلات على
  `users.py`/`user_service.py` رغم إنها كانت محفوظة على القرص — اضطررت
  أعيد تشغيل السيرفر يدوياً. لو واجهتوا سلوك API قديم غير منطقي، جرّبوا
  هذا أول شي. التالي: مراجعة تجربة الاستخدام الكاملة، أو قفل صفوف
  المحفظة، أو أي أولوية جديدة.
- **2026-07-23 — جلسة سحابية (تنفيذ توصيات المراجعة)**: نفّذت أغلب
  التوصيات من مراجعة المستشار التقني السابقة: (1) `app/core/rate_limit.py`
  جديد — قفل 15 دقيقة بعد 5 محاولات دخول فاشلة على نفس الحساب (in-memory،
  يحتاج Redis لو صار النشر multi-instance مستقبلاً)، (2) `CORS_ORIGINS`
  صار إلزامياً في `Settings` (كان افتراضياً `["*"]` بصمت)، (3) منع إنشاء
  أكثر من طلب استرداد pending/approved واحد لنفس الطلب (409)، (4) تعليق
  تحذير بارز أعلى `docker-compose.yml` يوضّح إن أسراره للتطوير المحلي
  فقط. أضفت اختبارين جديدين (23/23 ناجح). لم أنفّذ: قفل صفوف قاعدة
  البيانات لمحفظة الوكلاء (row locking) — مؤجَّل لأن SQLite (المستخدَم
  في الاختبارات) لا يدعم `FOR UPDATE` بنفس سلوك PostgreSQL، يحتاج تصميم
  ومسار اختبار منفصل قبل التنفيذ. التالي: قفل صفوف المحفظة (إن احتجناه
  فعلاً تحت حمل حقيقي)، أو بناء شاشة "حسابي".
- **2026-07-23 — جلسة سحابية (مراجعة تقنية شاملة)**: راجعت الـ backend
  كاملاً بصفة مستشار تقني (أمان، منطق مالي، بنية). لقيت وأصلحت باگين
  ماليين حقيقيين في `refund_service.py`: (1) ما كان فيه سقف يمنع طلب
  استرداد بمبلغ أكبر من قيمة الطلب الأصلية، (2) تنفيذ الاسترداد
  (`process_refund`) كان يغيّر حالة الطلب إلى `refunded` مباشرة بدون
  تسجيل السطر في `order_status_logs`، بما يخالف قاعدة "كل انتقال حالة
  يُسجَّل" المذكورة في README. أضفت اختبارين في `tests/test_refund_service.py`
  (21/21 ناجح الآن). التوصيات الأخرى (rate limiting على تسجيل الدخول،
  تحصين الأسرار الافتراضية في docker-compose.yml قبل أي نشر إنتاجي،
  منع تكرار طلبات استرداد معلَّقة لنفس الطلب) موثّقة لكن لم تُنفَّذ بعد
  — انظر آخر رد للمستشار في المحادثة للتفاصيل الكاملة. التالي: تنفيذ
  التوصيات المتبقية حسب أولويتها، أو بناء شاشة "حسابي".
- **2026-07-23 — جلسة سحابية**: أنشأت بيئة تطوير محلية كاملة (Python
  3.13 + PostgreSQL محلي + venv + قاعدة بيانات + `.env`)، شغّلت
  الـ backend والـ frontend محلياً، وبنيت الشاشات التفصيلية الثلاث
  المتبقية (الطلبات، الوكلاء B2B، العملات وسعر الصرف) بالكامل مع اختبار
  يدوي حقيقي في المتصفح عبر بيانات فعلية من الـ API. اكتشفت وأصلحت
  باگين: (1) تعارض أسماء ملفات حسّاس للأحرف كسر التطبيق بالكامل على
  ويندوز، (2) رصيد محفظة الوكيل ما كان يتحدّث فوراً بعد الإيداع. دفعت
  كل هذا العمل الآن. التالي: بناء شاشة "حسابي"، أو أي توجيه آخر من
  المستخدم.
- **2026-07-23 — جلسة سحابية**: أنشأت هذا الملف. آخر حالة كما موثّقة
  أعلاه. لم أبدأ بعد في الشاشات التفصيلية.
