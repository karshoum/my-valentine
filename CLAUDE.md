# وكالة براديس — تعليمات المشروع

منصة رقمية متكاملة لوكالة سفر وسياحة وخدمات أعمال (B2C & B2B). الخلفية
(Backend) مبنية بـ FastAPI + PostgreSQL بمعمارية طبقات نظيفة، تحت
`app/` (core / models / schemas / services / routers). التفاصيل
المعمارية والقواعد الصارمة (RBAC، التحكم اليدوي بسعر الجنيه السوداني،
دورة الدفع والتحقق اليدوي، محفظة الوكلاء...) موثقة في `README.md`.

دليل تصميم الواجهات (عند البدء في أي Frontend) موجود في
`docs/UI_DESIGN_GUIDELINES.md` — راجعه قبل كتابة أي شاشة.

## تنسيق العمل بين أكثر من جلسة (سحابة + جهاز محلي)

هذا المشروع يُعمل عليه أحياناً من جلسة سحابية وأحياناً من جهاز المستخدم
المحلي، وليس بالضرورة بالتناوب المنتظم. **قبل بدء أي عمل جديد في أي
جلسة**: نفّذ `git pull origin claude/wakalat-bradis-project-7iwyup` ثم
اقرأ `PROGRESS_LOG.md` في جذر المشروع لمعرفة آخر حالة وما تم إنجازه.
**بعد إنهاء أي دفعة عمل** (قبل أو بعد الـ commit): حدّث قسم "آخر حالة
معروفة" في `PROGRESS_LOG.md` وأضف سطراً جديداً في "السجل الزمني" يوضّح
ماذا أُنجز وما هي الخطوة التالية المقترحة، ثم ادفع (push) كل شي معاً.
لا تحذف السطور القديمة من السجل الزمني — فقط أضف فوقها.

---

## توجيه جودة ونظافة وتقسيم الكود (يُطبَّق بصرامة على كل كود Backend/Frontend)

### 1. التقسيم الصارم للملفات (Strict Modularization)
- يُمنع تماماً كتابة الكود في ملفات ضخمة أحادية (Monolithic Files).
- كل ملف لا يتجاوز 150–200 سطر تقريباً. إذا كبر الملف، قسّمه إلى ملفات
  أصغر بمسؤولية واحدة واضحة.
- افصل دائماً: المسارات (Routers/Routes) عن منطق العمل (Services) عن
  النماذج/الأنواع (Models/Schemas/Types). لا تُكتب منطق عمل داخل ملف
  راوتر، ولا استعلامات قاعدة بيانات داخل ملف Schema.

### 2. كود موثّق ذاتياً (Self-Documenting Code)
- أسماء تعبيرية كاملة للمتغيرات والدوال (مثال: `calculate_agent_discount`
  وليس `calc_disc`).
- Type Hints كاملة في Python (كل معامل والقيمة المُعادة)، وTypeScript
  Interfaces/Types كاملة في React لكل الـ props والبيانات.
- Docstring قصير أعلى كل دالة: هدفها، مدخلاتها، ومخرجاتها بوضوح.
  (هذا استثناء صريح من التفضيل الافتراضي بعدم كتابة تعليقات — في هذا
  المشروع تحديداً كل دالة تحصل على docstring موجز.)

### 3. تحديد المسار أعلى كل كود مُولَّد
- عند توليد كود لملف، اكتب مسار الملف الكامل في أول سطر:
  `# File: app/services/order_service.py` (Python) أو
  `// File: src/components/orders/OrderStatusBadge.tsx` (TS/React).

### 4. كود كامل وجاهز للتشغيل دائماً
- ممنوع أي Placeholder مثل `// TODO: implement this` أو `... rest of code`
  أو دوال ناقصة. كل كود يُسلَّم كاملاً وقابلاً للتشغيل فوراً.
